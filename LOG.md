# LOG.md

*Dated, append-only, one entry per session, max 5 lines each. Log changes, not activity. Newest at the bottom.*

- **2026-08-22 · Kickoff session (Claude Code, Fable 5, 22:30 to about 00:30).** Folder created from zero: `AGENTS.md`, `CLAUDE.md`, `STATE.md`, `NOW.md`, `DECISIONS.md`, `LOG.md`, `HANDOFF.md`, the six `playbook/` files (business-brief, accounts, launch-plan, landing-page, ads-program, separation), `research/` with five Opus reports (01 taxi lessons, 02 business and market incl. the KBO candidate HB Débouchage BV 1013.051.865 and the GBP address verdict, 03 landing page and ads, 04 Claude Code setup, 05 account separation) and `INDEX.md`, the three skills `/pro-orch` `/pro-orch-mid` `/pro-eof`, `save-to-cloud.cmd` (adapted from taxi), `first-time-setup.cmd`, `.gitignore`. Tally intake `9qAjYV` read and filed. Domain check: `pro-debouchage.be` and `prodebouchage.be` taken (OVH parking), `debouchage-pro.be` taken. Nothing bought, nothing created online, nothing in `../taxi-business/` changed (two research agents each ran one read-only `git status` there, nothing written, disclosed). Fady decided in the kickoff widget: new .be domain (name pending, recommended `prodebouchage24.be`, 11 candidates checked free, 9 more found taken), agency Cloudflare, video visit Sunday 2026-08-23 afternoon, ads at 15 to 20 euro a day plus the Google credit, and the zone correction (40 km around Wemmel, Brussels city excluded). Twelve decisions made, three open (`DECISIONS.md`). Open work in `NOW.md`.
- **2026-08-23 · Day 2 (three sessions, Fable 5, 09:30 to about 16:00).** Roro call 10:00 filed (business-brief section 9, prices from research/07 accepted, depot address decided, H1 decided). Company verified in the KBO (PRO DEBOUCHAGE SRL, BE 1027.454.187). Domain prodebouchage24.be bought at Infomaniak (12,17 euro) and the holder traded to Roro as individual (15,61 euro) after DNS Belgium refused the company; itsme then failed three times with identical data (holder phone fixed to his itsme mobile), eID app dead, front of Roro's ID uploaded for the manual check, support mail sent; holder-to-Fady fallback decided for Tuesday evening. Cloudflare zone with TXT, MX, SPF; Workspace Starter trial with info@ (Roro admin) waiting on DNS. Site v1 built (FR, NL, legal, 88 KB, no JS) and deployed to https://drain-prodebouchage.pages.dev (noindex on pages.dev); canvas v1 approved as base. Evidence checklist assets/prepared/evidence/README.md. Research 06 and 07 added. Closed NOW lines: Roro call, .be name pick, Chrome profile, town list (one call kept open). Decisions: nineteen dated lines in DECISIONS.md today, incl. one reversed the same day.
- **2026-08-23 · Design-prep session (Claude Code, Fable 5, about 17:30 to 21:30, six Opus research agents).** Research 08 to 13 added (Claude Design mechanics, anti-slop rules, plumber page teardown, copy masters and Belgian register, image catalogue, tech standard). Finding: claude.ai/design shows no Fable 5 picker (Opus 4.7); `/design` in Claude Code is the Fable 5 route, used here. Created `design/` (pack: master prompt, locked copy FR and NL v2, DESIGN.md, tech standard, asset manifest, follow-ups, README) and `design/canvas-v2/` (page-template.js, make-canvas.js, build-site.js). Magnific (about 1,400 credits): logo vectorised, van cut out and rendered in a studio from its real photo, hero and driveway upscaled, two muted clips; no generated before/after. Web exports in assets/prepared/web/. Design system pushed to a NEW Claude Design project "fady.be / Drain / Pro Debouchage" (31 files). v2 canvas saved: https://claude.ai/code/artifact/1f9a2cc8-0f37-4880-b300-9303f6a4e56f. `site-v2/` built and deployed to the new Pages project drain-prodebouchage-v2 (https://drain-prodebouchage-v2.pages.dev, noindex, headers and CSP verified, no external request, AVIF 8-bit after a 10-bit decode scare). v1 untouched. Decisions: placeholder reviews in design files only, generated imagery within rule 1, `design/` folder proposed, route B proposed. NOW updated (Fady review, publish the DS, four questions for Roro, placeholders out before Roro). Nothing run with git.
- **2026-08-23 · Design round 1 (Claude Code, Fable 5, evening, /pro-orch-mid, no agents).** Fady's eight reactions applied to `design/canvas-v2/page-template.js` and `design/ds-bundle/styles.css`: short pun chip (FR "On débloque la situation", NL "Wij maken het weer vlot"), all CTAs one height (56 px, weight 800), sticky bar floats with padding, header sticky on all screens, hero rebuilt as a 5-photo bento collage (car-2, lifestyle-1, -9, -11, -12-Cropped; new exports hero-van-side, hero-job-manhole, hero-camera-tablet, hero-job-drain, hero-machine in assets/prepared/web/img), titles to weight 900, PLACEHOLDER label removed (data-placeholder kept), Roro's name off the page, Afram corrected to Afrem in all live files (AGENTS, STATE, NOW, playbook, design pack), Fady's favicon wired in (favicon.svg plus regenerated touch/PWA icons). Rebuilt, redeployed to https://drain-prodebouchage-v2.pages.dev, canvas re-seeded and republished to the same artifact. Layout verified at 375 and 1280 by DOM measurement (browser pane hidden, no screenshots). One decision filed. `.claude/launch.json` added for the local preview server. Follow-up same evening: Fady saw stale styles on the preview (browser had cached style.css for 24h); build-site.js now stamps a content hash on the stylesheet URL so every deploy busts the cache; rebuilt and redeployed. Round 2 same evening: sticky-bar WhatsApp reduced to icon only (aria-label kept), mobile hero reordered to headline, CTAs, then collage; redeployed and canvas republished. Fady: foundation good, more refinement rounds coming this week. Round 3: hero collage cut to three photos (Fady's enhanced `assets/raw/pro-debouchage-car-2-enhanced.png` 1764x992 as the 16:9 wide, lifestyle-1 and lifestyle-12 as two 4:5 below; new exports hero-van-wide, hero-job-1, hero-job-2, AVIF 23 to 89 KB), stacked on mobile, two-up from 700 px; prices grid changed from four in a row to 2x2 (max 860 px). Old five-tile exports (hero-van-side, hero-job-manhole, hero-camera-tablet, hero-job-drain, hero-machine) are now unused in assets/prepared/web/img. Round 4: price cards stretch the full wrap (max-width cap dropped), WhatsApp button shortened to "Envoyer une photo" / "Stuur een foto" with the full aria-label kept, the line under the buttons cut to "Vous parlez à la personne qui envoie la camionnette." / "U spreekt met de persoon die de bestelwagen stuurt." and centred on mobile (the no-surcharge promise still stands in the final CTA), desktop hero trimmed (h1 cap 3.25rem, media column 470px, tighter padding) so the whole hero ends at 739 px and fits an 800 px and even a 760 px viewport. Copy files 02 and 03 updated to match the shipped strings.
- **2026-08-23 · Design round 5, English added (Claude Code, Fable 5, evening, one Opus 5 agent).** WhatsApp button now "Envoyer une photo maintenant" / "Stuur nu een foto" / "Send a photo now"; the line under the hero buttons removed everywhere. Language switch rebuilt as a brand segmented pill (teal-soft container, filled --ink current segment, 44 x 36 px targets, endonym as visually hidden text) and the header logo cut to 88/104 px to balance it; header now 90 px on desktop, 80 px on mobile. THIRD LANGUAGE: `/en/` built from `design/canvas-v2/copy-en.js` and `meta-en.js` (agent-written, 74 keys, key-for-key with FR, verified by script), annotated copy in `design/04-copy-en.md`. `build-site.js` generalised: `head()` takes a path map, hreflang and sitemap are generated per language, chooser and 404 are three-way, and the build drops EN cleanly if the copy files are absent. Canvas gained an EN artboard (5 total). Deployed and republished. Design doc 04 updated (header, switcher, call bar, breakpoints).
- **2026-08-23 · Design round 6 (Claude Code, Fable 5, evening).** Header logo is now Fady's monogram alone, no wordmark: `assets/prepared/web/img/logo-icon.svg` generated from `assets/raw/favicon.svg` with the white plate stripped and the box cropped to the mark; sized by height (40 px mobile, 48 px desktop), so the desktop header dropped from 90 to 73 px. The footer keeps a light plate behind it because the monogram has white counters. Hero eyebrow replaced by three chips (`chips` array, FR "24h/24 / Prix à l'avance / Caméra comprise", NL and EN equivalents); all three fit one row at 375 px in all three languages. WhatsApp button back to "Envoyer une photo" and a short supporting line returned under it ("Avec une photo, ça va plus vite."). CTAs rebuilt as an equal-track grid: one column on phones, two equal columns from 700 px, both buttons always filling the row and always the same width (294 px at 768, 271 px at 1280), in the hero and the final call alike. Header confirmed sticking at top 0 on scroll at every width. NOT DONE, on purpose: Fady asked for a "+20y experience" chip; AGENTS.md rule 1 names "20 years" as a forbidden unproven claim and the company was founded 2025-09-10, so the chip was not written and the point was put back to Fady.
- **2026-08-23 · Design round 7 (Claude Code, Opus 5, evening).** Three fixes from Fady's screenshot. (1) CTAs really stretch now: the `max-width` caps on `.actions` (460 px, and 600 px above 700 px) were still clamping the pair, which is exactly what his screenshot at about 653 px showed; caps removed, and `.s-ink .btn-call{max-width:460px}` in the page extras was rescoped to `.s-ink .wrap > .btn-call` so it only holds the standalone scam button and no longer squashed the final-call pair (was 460 vs 530, now 530/530). Verified: 343/343 at 375 px, 621/621 at 653 px, 271/271 in the hero and 530/530 in the final call at 1280 px, always flush with the container. (2) Header is now `position: fixed` (was sticky, which the canvas artboards do not honour), with `--header-h` (68 px, 76 px desktop) driving `body` padding-top and `html` scroll-padding-top; the chooser and 404 carry no header so their inline body style is `padding:0`. Bottom border removed. (3) Logo down another notch, 36 px mobile and 44 px desktop; note the page-extras block was overriding the design-system rule, so both had to change.
- **2026-08-23 · Design round 8 (Claude Code, Opus 5, evening).** Header "still scrolling" traced to the CANVAS, not the site: the live page was already `position: fixed` and held at top 0 through 6000 px of scroll (measured on drain-prodebouchage-v2.pages.dev), but each canvas artboard rendered the whole page at full height, so there was nothing to scroll inside the frame and panning moved the header with it. Fix: the four review artboards are now real device viewports (375x812 mobile FR/NL/EN, 1440x900 desktop) so the page scrolls INSIDE the frame like a phone and the fixed header pins; the full-page artboard is kept below them, relabelled, for whole-page review. Proven with a 375x812 iframe simulation of an artboard frame: header top stayed 0 at scroll 0, 900 and 4000. Chips and language switch recoloured from the logo monogram: three light tints with same-hue ink, declared once as `--lg-blue`, `--lg-mint`, `--lg-amber` (+ `-ink`, `-soft`) in styles.css; chips cycle blue, mint, amber, the switch uses the blue family with a filled current segment. Contrast checked: 4.77 to 6.06 on the chips, 5.22 on the current segment, all AA. Logo down again to 32 px mobile and 40 px desktop, header height token to 64/72 px.
- **2026-08-23 · Design rounds 9 and 10, plus the audit close (Claude Code, Opus 5, evening).** Round 9: desktop hero image block cut from 470 to 400 px so the hero ends 87 px higher; chips reverted from three logo tints to one shade. Round 10, on Fady's call: chips and language switch back to the HOUSE TEAL, logo back to 36/44 px, header token back to 68/76 px, the unused logo blue/mint/amber tokens and `--ink-2` removed (monogram hues kept as a comment). THREE READ-ONLY OPUS 5 AUDITS ran (build health, copy and rules, live site). Bugs fixed the same session: the footer privacy link was a dead `href="#"` on all three languages while both privacy pages were built and reachable only from the sitemap (now `/fr/confidentialite.html`, `/nl/privacy.html`, EN borrows the FR one with lang/hreflang); the skip link on both privacy pages pointed at a missing `#contenu` (the id is now emitted); `links[1]` dead data replaced by a single `privacy` key in all three copy objects; duplicate logo rules and two no-op rules removed from the page-extras block; dead `.tiles` 4-column rule and `.btn-sm` removed; `.tiles-3` breakpoints moved into the design system; the `.grid-2` wrapper around a single figure simplified; orphan `apple-touch-icon-180.png` copy dropped from `assets/img/`; unused exports trimmed; `Tervueren` corrected to `Tervuren`; the FR meta description and og:title given the narrow no-break space before `?` like the body copy; `Afram` corrected to `Afrem` in `design/ds-bundle/components/WhoComes.html`. New safety net: `build-site.js` now prints a loud WARNING on every build naming how many placeholder reviews are still in each language. Deployed and canvas republished after each round. One more found at the close by direct check: Cloudflare Pages 308-redirects /x.html to /x, so every privacy link, canonical, hreflang and sitemap entry was taking a redirect hop and the canonical did not match the served URL; all of them now use the extensionless form the host actually serves (the files stay .html). Everything the audits raised that is Fady's call is in NOW.md and DECISIONS.md, not silently changed.
- **2026-08-23 · Post-close addendum, live-site audit landed after the session closed.** It CONFIRMS the fixed header independently: `position: fixed` and `top` exactly 0 at scroll 500, 2000 and 6000, across all nine combinations of the three languages and 375 / 768 / 1280. Also clean: every security header served byte-for-byte as `_headers` intends, `X-Robots-Tag: noindex` on every path, zero third-party requests, zero cookies, zero console output, all 76 asset URLs per page returning 200, no horizontal scroll anywhere, every image with alt, one h1 per page, no skipped heading levels, and 186 text nodes per page checked for contrast with zero failures (minimum 4.54:1). Free wins applied and redeployed the same night: the stylesheet is now `max-age=31536000, immutable` (its URL already carries a content hash), `/favicon.svg`, `/apple-touch-icon.png` and `/site.webmanifest` gained a week of caching instead of falling through to no caching at all, and the self-hosted font is preloaded so it no longer costs a round trip after the CSS parses. Left for Fady: the image encodes are 2 to 4 times heavier than they should be (a mobile page is 646 to 892 KB, and `chambre-visite-jardin-800.avif` alone is 195 KB), which is a quality-versus-weight call, not a bug.
- **2026-08-24 · Day 3 (Claude Code, Fable 5, /pro-orch, five Opus agents plus one audit).** DNS Belgium replied (SR00287775): org field empty, manual check needs Roro's ID plus a utility bill; documents collected, reply drafted by Claude, sent by Fady the same day; registry still shows no NS, Tuesday fallback stands. Page: third chip settled as "30 ans de métier cumulés"; three heavy AVIFs re-encoded (195/138/121 to 77/60/57 KB); twelve fake reviews out (honest card, then three written placeholder cards Paolo/Daniel/Élodie per Fady's call, stacked, no carousel); real EN privacy page, Cloudflare NEL disclosure, false Google-Fonts privacy claim fixed; four design docs refreshed; trilingual conversion audit (research/15) applied IN FULL, all 35 entries: one voice per language, EN "call in English" trust line, register named (BCE/KBO), softened competitor price, rewritten meta titles. Canvas re-seeded, notes updated. Ads build package drafted read-only (research/14); its 7 open questions deferred to a dedicated deep-research Ads session on Fady's call. Depot visit re-scoped to one trip (imagery plus GBP video) once domain and Workspace live. Nine dated decisions filed. Closed NOW lines: experience chip, image weight, EN privacy, NEL note, ads prep, copy-audit calls, review replacement.
- **2026-08-24 · Post-close addendum (same session).** Fady sent the DNS documents email (filed in NOW as the clock item). On his yes, the /pro-eof skill gained step 4: stop and confirm closed all background agents before the handoff. Roro-input collection decided (DECISIONS): Tally form extended to 17 questions (four added: TVA, insurance report, most-common-case, night answering, all optional, French), published; reviews and selfie-video stay a phone ask. HANDOFF updated to match.
- **2026-08-24 · Second addendum, Tally restructure (same session).** Form q486RO rebuilt per Fady's notes and republished: 10 optional questions, price questions as choice plus pop-up price field (3 logic rules), photos and reviews sections out, night-answering out (fact filed in business-brief with the 3-to-6 review-ready count). New NOW lines: before/after via Magnific (Fady) and the swipe reveal section (Claude, later). One decision line filed.
- **2026-08-24 · Third addendum, prices out of the form (same session).** Fady's rule restated: prices come from market research plus competitive positioning, Roro only confirms on the finished site. The form's price section removed (his own Tally edits kept: new title, trimmed intro, catch-all gone); form republished with 6 optional questions. New [Claude] task: derive curage, fosse and cave prices from research/07, extending the competitor data where it is thin.
- **2026-08-24 · Continuation, Roro's Tally answers (Claude Code, Fable 5, /pro-orch-mid, no agents).** Submission of 11:42 pulled from Tally q486RO and filed in business-brief: guarantee yes without a duration (align with competitors), RC insurance at AG Insurance, machines Rioned, insurance report only when the client asks, VAT "activated" per Roro. Verified same day against the register: VIES INVALID and KBO shows no VAT quality, so the go-live VAT blocker STANDS until the register confirms; Roro is asked for the activation date. Q6 (most-frequent-case sentence) unanswered, stays open. Domain registry polled: still no NS, Tuesday fallback stands. NOW rewritten: Tally line closed, new Claude task (insurance-report copy to "sur demande" plus name Rioned), guarantee derivation joined the price-research task.
- **2026-08-24 · Addendum, paused on Fady's ask (same continuation session).** Fady gave the go for everything today, then paused the session (out, no internet at the new place until he returns). Before the hold: insurance-report copy softened to on-request in FR, NL and EN source (page-template.js, copy-en.js), Rioned and the AG Insurance cover named in the who-comes text, three languages consistent, NOT built or deployed yet. The price-and-guarantee research agent was stopped mid-run, nothing written, relaunch on resume. HANDOFF.md rewritten with the resume order.
- **2026-08-24 · Resume after the pause (same continuation session, one Opus research agent relaunched).** Copy round SHIPPED: insurance report now on-request in FR, NL and EN (service card, block, FAQ), Rioned machine and AG Insurance RC cover named in the who-comes text; rebuilt, deployed to drain-prodebouchage-v2.pages.dev (verified live on /fr/), canvas re-seeded and saved to the same artifact after a conflict check against the live version (only our own edits differ). Session polls: domain still no NS; VIES still INVALID and KBO still without VAT quality, blocker unchanged. WhatsApp for Roro drafted (Q6 plus VAT activation date), Fady sends. Price-and-guarantee research agent running toward research/16.
- **2026-08-24 · Addendum, honesty wordings round (same session).** Research/16 landed (curage, fosse, cave prices plus guarantees, one Opus agent, 20+ sources; septic emptying is 21 percent VAT by default, a research/07 assumption corrected). Fady decided two of three go-live wordings: FAQ zone line now "les autres communes à environ 40 km autour de Wemmel" instead of the unsupported "80 communes" (applied in FR, NL, EN, deployed, canvas saved); camera claim keeps 120 to 180 with the proof filed as research/17. VAT wording held: Fady asked for concrete verification first; the 6 percent conditions verified on SPF Finances (over 10 years, private residence over 50 percent, invoiced to the occupant, declaration on the invoice since July 2022, otherwise 21). INDEX updated with 16 and 17.
- **2026-08-24 · Addendum, the page grew honest teeth (same session).** Fady's three decisions applied in FR, NL and EN, deployed and canvas saved: VAT note states the 10-year condition (prices incl. 6 percent, 21 said on the phone), footer VAT line aligned, 1-month guarantee note added, curage 249 and cave-first-hour 229 added as cards four and five (FAQ 1 and the "not listed" card updated to match), fosse septique held for the accountant. business-brief carries the ready-to-copy 6 percent invoice sentence and the published prices; DECISIONS has the five-part entry; NOW closed the honesty line and the price-derivation line.
- **2026-08-24 · Close of the continuation session (/pro-eof).** Fady's copy rule applied to AGENTS.md section 6 on his explicit ask: customer-facing text is written to convert in the page's per-language voice, never literally translated (also saved to Claude's persistent memory). Invoice question clarified and parked as a NOW open call: B2C invoices are exempt from Peppol so a designed template is possible; B2B must go through Peppol software since 2026-01-01, the accountant's domain. Research agent confirmed finished, nothing left running. HANDOFF rewritten for the next session.
- **2026-08-24 · Final close (same session, Fady's yes on everything).** Both rule improvements APPLIED: AGENTS.md gained the design/ folder line (section 3), rebuild-and-check after page edits (section 7) and round-done-when-deployed-and-verified (section 10); design/canvas-v2/README.md now carries the exact build and deploy commands. The canvas artifact is RETIRED from the workflow (Fady reviews the deployed preview only, artifact frozen); NOW, STATE and HANDOFF updated to match. New task filed: playbook/site-companion.md before the Roro walkthrough (DECISIONS). Session closed clean, no agents running.
- **2026-08-24 · Night design session opens (Claude Code, Fable 5, /pro-orch-mid, no agents).** Q6 decision applied: the most-frequent-case sentence removed from the WC-et-évier card in FR, NL and EN (card kept, no replacement claim); the card template now skips an empty description. Rebuilt, deployed, verified on the preview in all three languages. Fady reacts live on mobile the rest of the evening; rounds batched.
- **2026-08-24 · Addendum, hero-ticker variant on its own URL (same night session).** New hero built behind a HERO_TICKER build flag: text left, promise right, and a full-bleed ticker of 12 real job photos (3 orphan images rescued: shower-drain snake, inspection tablet, van in garden) that drifts slowly and speeds up with scroll; on mobile it splits into two rows ticking in opposite directions; reduced-motion gets a static scrollable strip. Deployed to https://hero.drain-prodebouchage-v2.pages.dev (branch "hero", same Pages project, noindex). The classic design stays untouched at drain-prodebouchage-v2.pages.dev as the fallback, verified after the deploy. Source: page-template.js (opts.ticker), copy-en.js, styles.css, build-site.js; output site-v2-hero/, never edited by hand.
- **2026-08-24 · Addendum, ticker tuned on Fady's reactions (same night session).** Ticker is now scroll-only (no idle drift, glides to a stop); desktop hero text smaller (2 lines) and wider per Fady's screenshot, sub top-aligned in a 320px column; mobile ticker images up to 190px with the button radius (12px). Deployed to the hero URL and verified mobile and desktop.
- **2026-08-24 · Addendum, hero variant rebalanced (same night session).** Desktop scale quieted on Fady's reaction: logo 34px, header CTA 42px, H1 38px, hero CTAs 48px capped at 540px, chips smaller, column gap 96px, sub 19px semi-bold and vertically centered against the left block. Ticker back to automatic slow drift (21px/s), scroll coupling removed. Scoped to the variant via a header-t class; classic design untouched. Deployed to the hero URL and verified.
- **2026-08-24 · Addendum, hero fine-tuning and sanity check (same night session).** Fady's reactions applied over several rounds: photo-under-button line removed from the ticker hero (FR, NL, EN), sub settled at 21px ink with the yellow marker on the full price sentence, H1 fixed at 38px on desktop (wraps instead of shrinking), nav-to-hero gap tightened, hero restructured to two flex columns (sub always centers, never pushes the CTAs; mobile keeps the conversion order via display:contents). Fady: the ticker hero beats the classic one, we continue from it. Sanity sweep passed: 79/79/79 key parity, no console errors, no third-party requests, no broken images or missing alts, JSON-LD and hreflang intact, privacy pages clean, classic site-v2/ untouched since round 0, mobile payload 266 KB.
- **2026-08-24 · Addendum, hero version SETTLED after A/B rounds (same night session).** Final hero on the hero URL: no chips (toggled off in CSS, copy kept), photo-tip pill under the CTAs (camera icon, chip-style, full-width on mobile), sub top-anchored in a fluid 280-340px column with the yellow marker on the price sentence (all screens), H1 flat 38px on desktop, header with langswitch next to the logo plus call button (number kept, Fady's call) and a WhatsApp text button, ticker slow auto-drift. Full check passed: 79/79/79 parity, header number restored in three languages, no console errors, no third-party requests, no broken images, aria clean, JSON-LD intact, all pages and assets 200, no horizontal scroll at 360/700/820/1000/1600, buttons one line everywhere, ticker rows split correctly at 820. Stale CSS comment fixed. Fady does a last responsiveness pass before the next section.
- **2026-08-24 · Addendum, ticker images deduped by eye (same night session).** Fady spotted repeats; a visual pass over every prepared image found the cause: camera-ecran = hero-camera-tablet and hero-van-wide = hero-van-side are crops of the same photos, and hero-job-1 = hero-machine = machine-haute-pression are one scene; the sunset van (van-photo) was wrongly excluded as a dupe. Ticker rebuilt as 11 truly distinct scenes (sunset van in, tablet crop and square van crop out), vans at positions 1 and 9, mobile rows split 6/5 with zero overlap, verified live on both layouts. Lesson filed: never judge images by filename.
- **2026-08-24 · Addendum, mobile carousel with Fady's image order (same night session).** Mobile hero images are now a swipeable full-width 4:5 carousel with a dot chip (native scroll-snap, dots sync on swipe, ticker stays desktop-only); the empty desktop-buttons container was hiding a 16px gap right of the language toggle on mobile, fixed. Fady set the slide order by raw names; mapped to optimized assets (car-2-enhanced, lifestyle-1, -12, -7, -11, -9, -8, then wc/chambre/allee). New prepared asset: van-garden-square 480/800 avif+webp+jpg generated with sharp from assets/raw/pro-debouchage-car-2-enhanced.jpeg (the old 500px square was too soft). Verified live: order exact in FR/NL/EN, first slide serves the new 800 avif, 10 dots, desktop ticker untouched.
- **2026-08-25 · Domain unblocked (Claude Code, Fable 5, evening).** Fady's screenshot showed DNS Belgium validated the holder 2026-08-24 16:29, yet whois still had ZERO nameservers and NXDOMAIN everywhere. Root cause found in Fady's Chrome on his "do it": Infomaniak's NameServers page had "Customised NameServers" with BOTH fields empty, the 23rd's save never stuck, so nothing was ever pushed to the registry. Claude entered hera and olof .ns.cloudflare.com and confirmed (Infomaniak: "updated successfully"); whois.dns.be lists both nameservers minutes later. Left running: a 30-minute resolver poll; then Cloudflare zone flips Active and Workspace domain Confirm unblocks. STATE.md and NOW.md updated; Jordy call and holder-to-Fady fallback both dead.
- **2026-08-25 · Night design session CLOSED (Claude Code, Fable 5, /pro-orch-mid to /pro-eof, no agents).** The hero variant is the design we continue from (Fady). Final state on the hero URL: new hero with marker sub and tip pill, 11-photo desktop ticker plus counter-scrolling trust band (full copy), mobile swipe carousel in Fady's order (van-garden-45 lead, glass dot bar, 2.5s autoplay killed by first touch), static trust on phones, trimmed prices gap on desktop. Parked behind source toggles: chips, mobile trust ticker, short trust copy. Image lesson banked: two crop-duplicates shipped because files were judged by filename; full visual audit fixed the set to one image per real scene. Close sweep clean: 82/82/82 key parity, both build paths compile, all assets 200, no console errors, no third-party requests, responsive checks pass; classic fallback untouched all night.
- **2026-08-25 · Morning session, paused on Fady's ask (Claude Code, Fable 5, /pro-orch, no agents).** Domain CONFIRMED live at public resolvers (NS, MX, SPF, verification TXT); VAT registers polled, still blocked (VIES INVALID, KBO no quality). EMAIL CHAIN SHIPPED in Fady's Chrome on his go: Workspace domain verified (TXT flow, host Other, Cloudflare one-click refused per playbook), Gmail activated, DKIM 2048 generated and turned on, DKIM + DMARC p=none added in Cloudflare and verified resolving. Business Profile creation stopped mid-flow on Fady's pause, nothing typed or submitted; lookalike sweep clean (no profile with the phone, none named Pro Débouchage); 20-town service area decided (DECISIONS). Housekeeping: 35 orphan image files deleted (references checked, both builds verified), both rule improvements applied (accounts.md registry-whois line, AGENTS 7 viewed-images line), reviews asks re-tagged DO NOT CHASE (Fady). WhatsApp draft for Roro handed to Fady (domain news, accountant outcome, example invoice).
- **2026-08-25 · 40-minute afternoon session (same day, Claude Code, Fable 5, no agents).** Roro's example invoice landed, renamed to assets/raw/facture-exemple-17803.pdf and analyzed: made in Microsoft Word (365 FR), 6 percent VAT charged with correct math, TVA number printed, the mandatory 6 percent attestation sentence MISSING plus legal gaps (no seat address, no SRL); client in 1080 Molenbeek, so Brussels jobs are accepted when they come; findings in business-brief, a redesigned Word template is feasible (Fady: we will make him one, how he edits it TBD). THE BUSINESS PROFILE WAS CREATED in Fady's Pro Debouchage Chrome: Pro Débouchage, Drainage Service, no visitable location, depot address hidden, the 20 decided towns (Asse and Halle picked by eye past foreign homonyms), phone, WhatsApp chat link, website blank, Verify Later, gauntlet and 400-euro credit skipped, updates No. Google confirms: not publicly visible until verified. Depot visit now fully unblocked.
- **2026-08-25 · Addendum, consent + tag layer built behind a flag (same afternoon session).** The full go-live measurement layer now builds from the same source only when ADS_TAG_ID is set (plus optional ADS_CALL_LABEL / ADS_WA_LABEL): Consent Mode v2 default-denied stub first in head, trilingual banner with equal Tout refuser / Tout accepter (native copy, new keys in page-template.js and copy-en.js), assets/js/consent.js (localStorage pd_consent, 182 days, versioned, refuse wipes _ga*/_gcl*, reopen link in the footer), tag-day footer credit, tag-day privacy sections (legal.js markers + COOKIES_TAG), Google CSP swap in _headers, and call/WhatsApp click events with send_to conversions. Verified on a local flag-on test build in FR, NL and EN: zero non-self requests before choice, refuse stores and stays clean, accept injects gtag, clicks push the right events and labels. Without the flag the output is BYTE-IDENTICAL to the deployed hero preview (hash-proven), so nothing dormant ships (research/13 8.3) and no redeploy was needed. Build commands documented in design/canvas-v2/README.md; two NOW lines closed.
- **2026-08-25 · Evening continuation (Claude Code, Fable 5, /pro-orch-mid, no agents).** VAT registers re-polled after the accountant's VAT-office day: VIES still INVALID (21:31), KBO still no quality (snapshot dated 24/08), blocker stands. WhatsApp draft handed to Fady (domain and email live, profile created and hidden, accountant outcome ask, the 6 percent sentence ready to copy). CONSOLIDATION on Fady's call: the hero variant is THE site, built into site-v2/ with no flag and deployed to the new Pages project prodebouchage24.pages.dev; FR page byte-identical to the proven hero build, NL and EN verified, noindex and CSP headers confirmed on the wire. Old v2 project and its hero branch retired (deletion awaits Fady), site-v2-hero/ removed, parked options documented in design/canvas-v2/README.md. Design-continuation prompt handed to Fady.
- **2026-08-25 · Late-evening close after Fady's call with Roro (same continuation session).** VAT: the accountant called the VAT office, all in order, about 2 days to sync; blocker lifted per Fady, polling continues as confirmation. Invoicing fact corrected: Roro's WIFE invoices, Word for B2C and Peppol for B2B; a dedicated-session task opened to research and deliver a fully legal branded Word template (NOW). Roro told about the 6% sentence on the call, so the WhatsApp draft is superseded. NOW, STATE, business-brief and DECISIONS updated; HANDOFF rewritten for the ~2-hour design session that fires next.
- **2026-08-25 · Addendum (same continuation session).** Backup gap found and fixed on Fady's yes: /design/ (the whole page source) was missing from the .gitignore whitelist since the folder's creation, so no backup ever covered it; !/design/ added, git now tracks it, next save-to-cloud picks it up. Checked before the design session refires: the aborted session changed nothing (source mtimes clean, deployed FR page byte-identical to the local build). New NOW line: a LOCK-IN cleanup round after site v1 freezes the foundation and discards old projects and leftovers.
- **2026-08-26 · Night design session, the prices block (Claude Code, Fable 5, /pro-orch-mid to /pro-eof, no agents).** The price-note box is gone: surcharge and 60-euro lines already lived in the FAQ, TVA got its own FAQ entry in all three languages (9 questions now), the camera-value line dropped, and the guarantee became a yellow band under the price cards, finally carrying an animated seal (GARANTIE circling a bold 1). A floating guarantee badge (same seal) sits bottom right: click opens a small box with the band's words, any close fades it away for the visit, and it hides while the band is on screen. Detours tried and reverted on Fady's eye: big single-phrase rings, a top-and-bottom-arc seal, a scalloped stamp from Fady's Freepik reference, a band/CTA position swap. About 15 deploys, each verified on the deployed pages by DOM measurement; spin and scroll-hide need Fady's eyes because the hidden pane freezes animations and IntersectionObserver. VIES at close: still INVALID (2026-08-26 00:32). New in NOW: circle keep-or-drop verdict, the homeless 149-euro inspection-only price, and the CGV/terms-page task riding the invoice session.

- **2026-08-26 · Overnight v3 build (Claude Code, Fable 5, ~01:00 to morning, six Opus 5 research agents, Fady asleep).** On Fady's overnight instruction ("finish the website, new URL, agency-grade, research everything"): six research agents ran (customer psychology incl. the Halle-Vilvoorde scam prosecution, CRO masters, ~25 competitor sites plus Rioned, award-level design and typography, Belgian legal plus full CGV drafts, 2026 build tech), filed as research/18 to 23. Built `design/canvas-v3/` (copy FR/NL/EN rewritten native, new design system on Archivo Variable width axis, template, build) generating `site-v3/`, DEPLOYED to the NEW Cloudflare Pages project pro-debouchage-v3: **https://pro-debouchage-v3.pages.dev** (noindex; v2 preview untouched). New on v3: CGV pages FR/NL/EN (from the legal research, surcharges published in the price section, withdrawal exception, Livre XIX), before/after WC swipe slider (Nano Banana Pro from our own job photo, honestly captioned), new van-in-street image replacing the disliked studio van (generated from our real photos, plates blurred), equipment section naming Rioned UrbanJet / Rausch / Zebra (text, no logos: trademark), tenant/syndic/horeca block, scam section extended with the 265-victims fact and a point-by-point "we do the opposite" checklist, numbered service rows (no random card photos), 11-entry FAQ (camera-only 149 EUR placed in the camera FAQ as suggested; tenant question added), enterprise number printed WITHOUT the wrong "BE" prefix (legal flag). Placeholder reviews kept and marked; build still warns. Verified live by DOM at 375 and 1440 (pane hidden, no screenshots): no overflow, fonts loaded, slider and exclusive FAQ working, 20 tel links. VIES poll: still INVALID (2026-08-26 ~01:00). Magnific: 6 generations (~450 credits). Curage-VAT nuance flagged to the accountant (NOW).

- **2026-08-26 · Morning fix round on v3 (Claude Code, Fable 5, Fady's first review from bed).** All ten points applied and redeployed to https://pro-debouchage-v3.pages.dev: "Appelez. / Bel. / Call." added to the headline; headline edge-touching solved for good (the hero wrap is a size container, each language carries a measured cap, so the longest pain line always ends near 91 percent of the width at ANY phone width; fallback factors for old browsers); display weights raised to 900; eyebrow shortened to one line ("Vilvorde · Autour de Bruxelles · 24h/24" pattern); step numbers were missing entirely, now CSS counters in the squares; reveal animation removed from steps, services, segments and text blocks and triggered 18 percent BEFORE entering the viewport elsewhere, so nothing flashes; sticky-bar WhatsApp now filled teal and wider (68px) like v2; carousel dots spaced (13px gap); towns rebuilt as the v2-style stacked columns with yellow square bullets (two columns on phones); the v2 floating guarantee seal badge is back (identical behavior: opens, any close dismisses for the visit, hides beside the band); header no longer fixed, it scrolls away and the bottom call bar is the one persistent element; Dutch long compounds got soft hyphens (Hogedrukreiniging, terugkomgarantie, beroepsaansprakelijkheid, ontstoppingsdienst). Overflow sweep at 320 and 375 in FR, NL and EN: clean.

- **2026-08-26 · Review round 2 on v3 (Claude Code, Fable 5, Fady's desktop pass).** Deployed to https://pro-debouchage-v3.pages.dev: the desktop tickers were genuinely broken (rAF clone marquee left blank space); both are now pure CSS marquees (two sets, aria-hidden clone, keyframes, hover pauses, reduced-motion wraps) so they always drift. Service-number hover stroke replaced by a quiet lift (row tint, numeral rises 5px, title 2px). Steps section now animates visibly on arrival: the yellow line fills over 1.5s and the numbers light up in sequence (IO-triggered, works in every browser, reduced-motion instant). Review cards restyled: white rounded cards with soft shadow, solid gold stars, avatar gradients in the logo's colours. Towns never break (nowrap, wider desktop columns). FAQ opens and closes fluidly (WAAPI block-size animation, one open at a time, plain details without JS). Floating-badge popover restyled to v2's hierarchy (bold sentence-case title, single promise sentence, rounded) and the guarantee band got the same radius. Final call left-aligned with the yellow underline under the number; footer tidied (rounded plate, big underlined phone, yellow section label, separated link row). Vilvoorde de-emphasised everywhere per Fady's correction (zone-first eyebrow "Débouchage · Autour de Bruxelles · 24h/24", meta and zoneT rewritten in FR/NL/EN, chooser title); rule filed in landing-page.md and DECISIONS.

- **2026-08-26 · Review round 3 on v3 (quick pass).** Deployed: bolder overall type (body 430, leads 460, h3 850, service titles 870, price names 800 and amounts 900) with tighter tracking on the big uppercase (-0.028em display, -0.02em service titles and amounts, label spacing eased to .09em); desktop gap between the nav and the hero halved (46px); the line under the CTAs is about the photo again ("Avec une photo, on comprend le problème tout de suite, et on vous aide plus vite", NL/EN native equivalents; EN keeps its English-line claim); the chip is service-first per Fady's wording: "Débouchage pro et honnête · 24h/24" / "Pro en eerlijk ontstoppen · 24/7" / "24/7 pro and honest drain unblocking", one line at 375 in all three. Mid-round ask applied too: tickers never pause on hover, 16px between photos, the strip breathes (16px inner padding, 46px below before the trust band).

- **2026-08-26 · Review round 4 on v3 (the BOLD round).** Deployed: the whole type system went up a class in weight, intentionally: body 470, leads 520, every h3 900, service titles 900 at 92 percent width, price names 870, amounts 900, FAQ questions and buttons 870/850, trust and towns 720; display tracking tightened to -0.03em and the condensed width eased 76 to 80 percent so the strokes read thicker; per-language headline caps re-measured (FR 90, NL 85, EN 88 percent of the column at 375). Trust-band items rewritten as short punchy facts in all three languages so none can wrap on a phone ("Assurée chez AG Insurance", "Entreprise 1027.454.187"...). Service numerals re-seated on mobile (line-height .82 plus a small top margin) so the number tops align with the titles. The photo sentence under the CTAs is now bolder and darker. Carousel dots got 30px of clearance before the trust band.

- **2026-08-26 · Session close for continuity (mid-day).** HANDOFF.md rewritten for a dedicated section-by-section review session (v3 surface, round routine, the settled list from review rounds 1 to 4, the next-session mode). Day NOT closed; next session starts with /pro-orch-mid. Nothing else changed at close.

- **2026-08-26 · Section pass 1 on v3: header and hero (Claude Code, Fable 5, /pro-orch-mid, no agents).** Fady's first section-by-section notes applied and deployed to https://pro-debouchage-v3.pages.dev. TYPE: one weight ladder now governs the whole page (470 body, 520 lead, 600 emphasis, 750 small-caps label, 800 button and link, 900 every heading and number), replacing a scattered 600/650/720/750/800/850/870 set; the real cause of "v3 reads thinner than v2" was font-stretch below 100% on normal headings, so 90/92 percent narrowing came off the guarantee, scam, situation, tile and service titles (kept only where type must physically fit: the H1, the H2s, the giant numerals). Sentence-case headings share one tracking token, uppercase labels one more. CTAs: the hero pair is now one object in two colours, identical 64px height, 18px type, weight 800, padding, radius and seat, both full width on phones and both auto side by side from 56rem (measured equal, baselines flush). The line under the CTAs became a TIP, one short line at body weight with a single marker-highlighted word, native per language ("Astuce une photo, et on voit déjà le problème", NL and EN equivalents); the EN "you can call in English" claim moved into the trust band so the page did not lose it. SPACING: section rhythm cut from 56-110px to 44-84px, the photo strip now sits directly on the blue band (its own hairline is the seam, gap measured 0) and the first section under the band takes 64px instead of 110. GUARANTEE: v2's setting restored because it genuinely wins, sentence case at full width and 900, and the title finally says what the seal's "1" means ("Garantie 1 mois sur le débouchage" / "1 maand garantie op de ontstopping" / "1 month guarantee on every unblocking"); the seal got v2's 46px numeral and plate shadow; the legally required "adds to your legal rights" line (research/22, art. VI.83 14 degrees) stays, one step quieter, as its own copy key instead of a fragile string split. RADIUS: one ladder (10px buttons, 12px photos, 14px cards and bands), softer than v3's square edges and tighter than v2's, applied to buttons, call bar, language switch, ticker and carousel photos, tiles, split figure, slider stage, review, honest, dark price and popover cards, chips, labels and bullets. SERVICES: the row-wide hover fill is gone (it ran edge to edge behind "Appeler" and a stray touch left a phone row painted); the quiet lift survives inside @media (hover:hover), so touchscreens see no hover at all. FOOTER: white, no yellow rule, no logo plate (the monogram carries no white artwork, only its clip rect, so it sits unframed) and the logo up from 32 to 44px; labels teal, links ink. MID-ROUND ASK: the mobile sticky call bar now stays off screen while the hero CTA pair is visible and slides in once it leaves (IntersectionObserver, .js gate set by the head script so there is no flash, and no-JS keeps the bar visible as the safe failure). Verified on the deployed pages by DOM measurement in FR, NL and EN, no horizontal overflow, all six service titles still single-line at full width. NOT verifiable here and left to Fady's eyes: the bar's slide, the seal spin and every reveal, because the hidden pane reports visibilityState hidden, which freezes transitions and delivers no IntersectionObserver callbacks at all (all 18 reveal elements sat dormant, which is how the freeze was proven rather than assumed).

- **2026-08-26 · Section pass 1, follow-up round (same session).** Two faults Fady caught on the sticky bar, both fixed and redeployed. (1) The bar jumped instead of sliding: the CSS interpolated a percentage transform against `none`, which browsers do not animate reliably, hence the flash of page then the pop. Now translateY(calc(100% + 14px)) to translateY(0), a real interpolation. (2) The floating guarantee badge sat 86px up even when the bar was away, because its bottom offset was hard-wired to the bar's height. It now rests 14px off the bottom and is PUSHED up by exactly the bar's height, in the same 250ms and easing, so the two move as one object; the lift is a --cb-lift variable that composes with the badge's existing dismiss and hide transforms, so one transition covers all of it. The state class moved from the bar to <html> so both elements read it. Measured on the deployed page at 375: bar away, badge 14px off the bottom; bar in, badge exactly 14px above the bar. At 1200 the bar is display:none and the badge keeps its own 24px corner with no lift, even with the class forced.

- **2026-08-26 · Section pass 2 on v3: THE FONT WAS NEVER LOADING (Claude Code, Fable 5).** Fady said for the fourth round running that v3's type is thinner than v2's. It was, and no CSS number could have fixed it: `design/canvas-v3/styles.css` began with a UTF-8 byte order mark. That file is INLINED into `<style>`, where a leading U+FEFF is a stray token, so the CSS parser opened a bogus rule on it and swallowed the FIRST rule in the sheet, which is the main Archivo @font-face (unicode-range U+0000-00FF, i.e. essentially every character on the page). The browser fell back to Arial, which has no weight axis, so every declared weight from 600 to 900 rendered as one faux bold. Proven, not guessed: the live page parsed only 2 of its 3 @font-face rules and its "Archivo" produced just 2 distinct advance widths across weights 100 to 900 with font-variation-settings having no effect, while v2, measured in the SAME browser minutes apart, produced 9 distinct widths; the woff2 itself was confirmed a valid variable font by parsing its fvar table offline (wght 100..900, wdth 62..125), and the same file loaded manually via the FontFace API varied correctly, which isolated the fault to the stylesheet rather than the font, the server or the build. Fixed: BOM stripped from the file, stripped again defensively in build.js (a Windows editor can re-add it on any save) with an assert that throws if the sheet ever starts with something that would swallow rule one, and the whole story written into the head comment of styles.css. After the fix v3 reproduces v2's exact width table, 664.5 to 832.2. THEN the ladder was reset to v2's measured values, since Fady asked to lock v2's thickness: body 400, every lead 400 (all 7 identical, no section inventing its own bold sub text; emphasis is the .hl marker, which carries no weight of its own so it works inside a 400 lead and the 900 promise alike), 600 emphasis and small print, 800 buttons links and FAQ questions, 900 every heading and number. Page now uses exactly four weights, 400/600/800/900, nothing else. Guarantee band, popover and seal ported to v2 to the number (title 900 sentence case at 20px and 18px with -.02em, body 600 at 17px, seal numeral 900 at 46px). Headline caps RE-MEASURED because the font change invalidated the round-4 numbers, which had been tuned against Arial: at 375 FR 87, NL 93, EN 92 percent of the column; at 1280 FR 98, NL 94, EN 98 percent; no overflow in any language at either width. The Dutch desktop overhang filed in NOW this morning is GONE (94 percent, 66px of margin) because real Archivo at the narrow width axis is slimmer than the Arial that was standing in, so that NOW line was removed rather than actioned.

- **2026-08-26 · Section pass 3 on v3: sentence-case headings and the before/after layout.** (1) Every section heading came off ALL CAPS behind ONE switch: three custom properties at the top of styles.css (`--case-head`, `--track-head`, `--lh-head`) drive the H2s, the service titles, the scam and situation h3s, the equipment tile titles and the legal-page H1, with the exact revert values written in the comment beside them, because Fady said he may go back. Deliberately NOT switched and stated as such: the hero H1, which stays in caps as the signature, and the small labels (kicker, eyebrow, chips, footer legal label) which are labels rather than headings. Verified: 26 headings now sentence case, the only remaining uppercase heading is the footer's "Mentions légales" label, hero H1 still caps, and no H2 or h3 overflows its column at 375 in FR, NL or EN. (2) The before/after section was rebuilt because Fady called out the hole in it: measured, the text column was 133px tall beside a 575px slider, so 442px of the section was empty paper and the picture ran past the fold. The heading block moved INSIDE the left column (kicker, H2, rule, lead, caption) so neither column is a stub, the columns are 1.2/0.8 with the copy on the wider track since the slider is a tall 4/5 portrait, and they are vertically centred against each other so any leftover height splits above and below the text instead of dumping under it. The slider now fills its own column exactly (422 by 528) instead of leaving a strip of paper beside it. Section height 960 to 696px, imbalance 442 to 220px split evenly. The 4/5 crop was left alone on purpose: both frames were opened and looked at (AGENTS section 7) and the bowl sits low in each, so a shorter aspect would have cut the very thing the section exists to show. Mobile still stacks, no horizontal overflow anywhere.

- **2026-08-26 · Section pass 4 on v3: the before/after slider was dead, and bigger headings on phones.** (1) THE SLIDER NEVER WORKED. Fady spotted that the "before" photo, which he thought he still had to create, already exists and was simply never on screen. Cause: `.ba-top`, the div wrapping the before frame, was `position:static`, and because the rule above it absolutely positions every image inside `.ba-stage`, that div collapsed to HEIGHT 0. Its clip-path then had a zero-height box to clip, so the before frame was clipped away entirely: the slider showed the after photo on both sides of the handle and dragging changed nothing. Fixed with `position:absolute;inset:0` so the clip has a real box. Shipped broken from the overnight build and missed in every check since, because earlier verification confirmed the range input responded and the handle moved, never that the visible pixels changed. Verified properly this time by hit-testing the deployed page: at 50 percent the left of the handle is ba-before and the right is ba-after, at 0 percent both sides are ba-after, at 100 percent both sides are ba-before. Lesson worth keeping: for a visual widget, assert on what is VISIBLE, not on whether the control fires. (2) Headings raised at the small end only, desktop caps untouched, all measured at 375: section H2 32.7 to 35.8px, service titles 22.5 to 24.8, generic h3 19.5 to 21.0, guarantee title 20 to 22.5, scam h3 19.4 to 20.6, situation cards 18 to 19, equipment tile titles 17 to 18. No heading overflows its column in FR, NL or EN and no page scrolls sideways; the hero H1 was not touched and still sits at 87 percent of its column.

- **2026-08-26 · THE LOCK-IN (Claude Code, Fable 5, four read-only Opus agents, then one editor).** Fady locked the reviewed design as THE design, named v1. Checked first, then acted: a dependency audit (the build's ONLY outside ties: canvas-v2/legal.js and assets/prepared/web/img, all 100 image files verified present), a full folder inventory, a docs consistency audit (every v1/v2/v3 reference in the live files, line by line), and a deployed health check (11 pages and 100 assets all 200; the two "findings", index-follow meta and the short EN terms, are both by design: noindex is scoped to the preview hosts in _headers, and the EN terms page is a courtesy summary naming FR/NL binding). Then the cleanup: legal.js copied INTO the design and the require made local (the one thing a blind delete would have broken); design/canvas-v3 RENAMED design/canvas-v1 and site-v3 to site-v1 with every internal path updated; a README written into canvas-v1 carrying the build, deploy and ADS-flag commands (ported from the v2 README before it left) plus the BOM warning, the fit-cap rule and the visible-pixels verification lesson; launch.json and .gitignore updated (site-v1 now inside the backup whitelist, which the old output never was). Legacy OUT of the working tree: design/canvas-v2, design/ds-bundle, the seven v2 pack docs and the old site/ moved to _archive/2026-08-26-lock-in/ (archived, not deleted, because git tracking could not be verified without running git); site-v2/, stray root dumps new.html and old.html, and the .wrangler cache deleted outright (regenerable). Proof the lock survived the cleanup: rebuilt from the cleaned tree (117 files) and redeployed; the live /fr/ serves "Generated by design/canvas-v1/build.js" with all pages, fonts and tel links intact. Docs brought to ONE story: STATE.md rewritten whole, NOW.md rewritten (closed: the v2-vs-v3 verdict, the floating-circle call, the v2 design-session line, the before/after build task; the header ruling now names the STATIC header as settled), AGENTS.md folder map and rule 5 now name canvas-v1/site-v1, accounts.md finally records the pro-debouchage-v3 Pages project as THE working project with the three retired ones listed for deletion, landing-page.md sections 2/5/6/9 rewritten to the locked page (S1 to S17), launch-plan and separation updated. Still Fady's: the backup double-click BEFORE anything else (folders moved today), the three old Pages project deletions and the keep-or-rename of the pro-debouchage-v3 project name.

- **2026-08-26 · Lock-in addendum, same session.** Fady's widget answers: delete all three retired Pages projects, keep the pro-debouchage-v3 project name until go-live. Deleted prodebouchage24, drain-prodebouchage and drain-prodebouchage-v2 via wrangler, verified all three hosts no longer resolve and the locked preview still serves 200. This also closes the old naming-breach acceptance (the breaching first-page preview no longer exists) and the "one Pages project per client" rule in separation.md is true again. accounts.md and NOW.md updated in the same session.

- **2026-08-26 · Evening, continuation session: source folder renamed, before-photo replaced, `to-delete/` born.** (1) Fady said the `canvas-v1` name kept reading as a review step he was supposed to open, which he never wants: it is only the code the page is built from. Renamed `design/canvas-v1/` to `design/site-source/`. The build script needed no code change (it resolves its own location); only comments, the README and the nine live files that named the old path were updated. `_archive/` and old LOG/DECISIONS entries keep the old name as history. Also answered his Cloudflare worry: the 500-per-month limit is Cloudflare's OWN build system, which we do not use, and direct uploads are not builds. Real limits are 20,000 files and 25 MiB per file per deployment against our ~117 files, so deploy-per-round costs nothing. (2) Fady recreated the before photo in Magnific and dropped it into the image folder. Fitted it to the after by MEASUREMENT rather than by eye: a scale-and-offset search maximising correlation with the after image, with the bowl masked out because its content legitimately differs. Result 920px wide, crop at 48,117. It matches the after at 0.749 where the old crop managed 0.506, and its tone lands within two points of the after's mean RGB (the old crop was six points brighter), so the wipe is tighter and flashes less than the pair Fady had already approved. Five derivatives regenerated at quality settings tuned to land near the old file sizes; source kept as `assets/prepared/magnific/ba-before-recreated.png`. (3) The old derivatives went to a new `to-delete/` folder, Fady's idea, now a rule in AGENTS.md section 3: superseded files are moved to a dated subfolder there and only Fady empties it. (4) Verified on the deployed page against the ORIGIN, not the browser: all five files serve 200 with the exact new byte counts, and the alignment measured on the live pixels reads 0.749. One trap found and written into the README: images ship with a 24-hour cache, and this tab was serving a stale AVIF while the deployment was correct, so an image swap must be judged after a hard refresh.

- **2026-08-26 · Evening, trust strip rewritten and a marquee bug caught behind it.** Fady's notes, all applied in FR, NL and EN. On a phone the strip is not a marquee at all, it is a stacked list, which is why "Prix dit au téléphone" and "Confirmé à votre porte" read as two separate claims sitting on top of each other: the second was a fragment of the first's sentence, so it is DELETED from the strip (it survives inside the hero sentence and the meta description, where it has a verb and makes sense). "Caméra comprise" became "Inspection caméra comprise", matching the service row name. "Garantie retour 30 jours" became "Débouchage garanti 30 jours": Fady was right that "retour" reads like a clothing returns policy, and 30 jours is kept rather than his suggested 1 mois because CGV article 12 says thirty days and the strip must not drift from the terms. "24h/24, 7j/7, fériés compris" became "24h/24, 7j/7, même les fériés", killing the second "compris". "Entreprise 1027.454.187" became "N° d'entreprise 1027.454.187", the official Belgian wording, and no "BE" prefix because the VAT quality is still not in the register. NL and EN follow the same promises in their own idiom, not word for word: NL keeps "terugkomgarantie" logic as "Ontstopping 30 dagen gewaarborgd" and "Ondernemingsnr.", EN uses "Registered company" and "30-day guarantee on unblocking". THE BUG BEHIND IT: each animated set translates -100% of itself, so a set narrower than the screen leaves an empty gap at the end of every cycle. One list was 1525px in FR, meaning the strip was already gapping on any screen wider than that and dropping an item made it worse. Each set now carries three copies of the list (FR 4574px, NL 4696, EN 5383, all clear of 4K) with only the first readable to screen readers, and `--tick-dur` went 46s to 138s so the speed is unchanged. Verified on the deployed page: all three languages one line per item at 375px with no sideways scroll, six items on mobile, no gap at any width up to 3840. The image ticker has the same latent bug at 2795px and is filed in NOW.md rather than fixed here. Same round, services heading: Fady was shown three alternatives against the current line and picked the calming one, so "Votre problème est dans cette liste." became "Pas de panique, c'est notre métier.", NL "Geen paniek, dit is ons vak.", EN "No panic. This is our trade." (the EN is deliberately not "this is what we do", which would have echoed its own kicker word for word). The six service rows themselves were not touched, Fady said they are fine. Live and verified in all three languages. THEN Fady caught the photo strip sitting half blank, with the tell that any mouse move or scroll made the missing photos appear: that is a SECOND and different bug from the width gap. A `loading="lazy"` image moved into view by a CSS transform never loads, because the compositor does not recompute intersections, and only 3 of the 22 strip images were eager; any interaction forces a recompute, which is why it healed itself the moment he touched anything. Fixed by watching the static `.ticker` container (it does not move, so the observer fires normally) and promoting its images once. Deliberately NOT fixed by making them eager in the markup: the strip is display:none under 820px, so that would have made every phone download 11 photos it never sees, and the mobile check confirms only 2 ticker files are requested at 375px. The whole strip is now fetchpriority=low so it queues behind the hero. Same round, the image ticker got the width fix too (each set 2795px to 5590px, two copies, 80s to 160s), so both marquees now clear 4K: photo strip 5590px at 160s, trust strip 4574px at 138s. The duplicate copies reuse the same 11 image URLs, so they cost DOM and zero bytes, and the repetitive HTML still compresses to 26 KB. Both failure modes written into the README so the next round does not reintroduce them.

- **2026-08-26 · Evening, the before/after section rebuilt as a three-photo story.** Fady's idea, built after an explicit understanding check. Looking at the images first paid off twice: the in-action photo we already had (`job-wc`) turns out to be shot in THE SAME toilet as the before and after (same Geberit plate, same cupboard, same stone floor), so the trio is genuinely one job in order rather than three unrelated photos, and it puts the technician, the Pro Débouchage hoodie, the phone number and a Rioned tool on screen where a visitor will actually see them. The folder Fady dropped also held a SECOND bathroom's before/after pair, which I flagged rather than used, and he confirmed it was for something else and deleted it. Built: an ordered list of three 4/5 frames, tilted a degree or so like photos laid on a table, yellow numbered tags reusing the H1 marker, hand-drawn ink arrows between frames (a pale under-stroke first so they stay readable where they lap 16px onto each photo), a drawn ring on the standing dirty water and a drawn arrow at the Rioned machine in the bowl. Both marks are drawn in a 400x500 viewBox that matches the 4/5 frame exactly, so they stay glued to what they point at at every width. The arrows sit at 52 percent height, not a third of the way down, because higher up the second one laps straight across the phone number printed on the technician's back. Copy rewritten per language, not translated, and the honesty caption now states plainly that photo 1 is a faithful recreation and photos 2 and 3 are our originals, cropped only. Fady's "New After" replaced the old after photo (old files to `to-delete/`, source to `assets/raw/ba-after-original.jpeg`); the pixel-alignment work the wipe demanded is now moot, which is why the better framing costs nothing. Removed with the slider: its JS handler, its handle icon and about 20 lines of CSS, so the section now ships zero script. REJECTED BY FADY the same evening, and rightly: my version was a tidy row of equal frames with thin pen strokes. He built the real thing in Figma and sent it, so the section was rebuilt to HIS composition: tilted stickers in brand colours at different sizes, overlapping, joined by his own brush arrows. His three Figma SVG exports were 1.7 to 2.2 MB each, because Figma traces a brush texture into millions of points, and even at svgo precision 1 they were 60 KB each; his PNG exports were 11 to 14 KB and convert to avif/webp under 6 KB, so the PNGs are what ship. build.js gained an alpha flag: those images fall back to PNG, never JPEG, which has no transparency and would ship a white box. Layout is positioned entirely in PERCENT inside one aspect-ratio box, so the collage scales as a unit and cannot come apart at an untested width: a portrait cascade under 56rem, a wide row above it, with the tall hook arrow swapped in for the zigzag on phones because it is the one that reads going downwards (the unused one is display:none, so it is never fetched, verified). Numbered tags and per-photo captions dropped to match his design; the kicker already carries Avant, pendant, apres. Sticker colours are this section own: #F3D75A, #26C4B4 and --star, a brighter yellow and turquoise than the page palette on purpose, sampled from his export. Then Fady's four refinements, all applied: arrows down to a hint (desktop 12.2 to 8.6 percent), the coloured border replaced by a DRAWN edge (a rounded rect roughened by a turbulence filter, thinner, sitting a fraction askew to the photo, so it reads brushed like his arrows instead of tracing the picture exactly), the white gap between photo and edge removed, and the copy rewritten. THE ONE THAT MATTERED MOST: on a phone the third photo was covering the blocked bowl in the first, so the opening photo showed a wall and told the visitor nothing. The bowl sits at about 20 to 40 percent across and 44 to 53 percent down, and that corner is now a hard constraint written into styles.css, with a check that asserts it. I broke it once mid-round by tightening the overlap and caught it before deploying the regression. Copy: the lead was chat between Fady and me ("the same toilet, the same job, in order") and is now written for someone who arrived from an ad with a blocked toilet, "une intervention chez un client : ce qu'on trouve, ce qu'on fait, ce qu'on laisse", NL and EN in their own idiom. The honesty line keeps the disclosure but leads with the point worth making: our own photos, our own technician, our own van, nothing bought from a stock library, the first one reconstituted from our original. Verified deployed at 375 and 1280 in FR and NL: bowl clear of both other photos, the cascade still overlaps, nothing overflows, no sideways scroll, alt text intact, no slider leftovers. FOURTH ROUND, same evening, on Fady's notes: the section copy is GONE (he said we were explaining ourselves, the headline carries it), the drawn edge moved OUTSIDE the photo instead of sitting on it, the section MOVED to sit between services and steps, and its photos now overhang the section boundary to bleed into the next band. The overhang works by shortening the collage box, not by moving the photos: box aspect went 80/115 to 80/90 on phones and 252/100 to 300/100 wide, which pulls the section edge up through the pictures without resizing anything, with the collage given z-index so it paints over the next background and the next section given 54px more top padding so its heading keeps its air. Measured live: 52px overhang on desktop with 84px clearance to the next kicker, 50px and 48px on a phone, nothing colliding. The section is now s-card so it merges with services above and the photos cross visibly into the paper of steps. HONESTY, flagged not buried: the before photo is an AI reconstruction and there is NO real photo of the blocked bowl anywhere in the repo (checked assets/raw, only other jobs), so deleting the section caption would have deleted the disclosure with it. The one-line note moved into the footer legal block instead (new photoNote key in all three languages), where it stays true without interrupting the sell. Fady to confirm he is happy with it there.

- **2026-08-26 · Evening, the preview alias broke and it is NOT the deployment cap.** After the fourth refinement round deployed cleanly, https://pro-debouchage-v3.pages.dev froze on one build while every new deployment kept working on its own hashed URL. Fady recognised the symptom from a previous project and asked whether we had hit the 100-deployment cap. We had not, and the evidence is conclusive: 20 old deployments were deleted (13 left, far under any cap) and the alias still did not move; then the exact deployment the alias was pinned to (3c402af9) was deleted with --force, and the alias STILL served that deployment's content and its ETag, for a build that no longer exists. Cache-busting query strings, no-cache request headers and three further deploys all changed nothing, and Cloudflare returns no cf-cache-status on that host. So the alias is serving a stale copy held on Cloudflare's side, not pointing at a wrong deployment, and nothing available from wrangler fixes it (there is no promote command for Pages, and delete only takes --project-name and --force, never --yes, which is what made the first delete loop fail silently twenty times). Work is unaffected: every round is live and verifiable on its own deployment URL. Decision went to Fady, who chose a fresh Pages project under a throwaway name so the go-live name is not burnt on a preview. Created `pd-review`, deployed, verified: all eleven pages 200, the custom 404 answers 404 with the right title, every new asset serves, and the noindex header applies. That last one needed a fix first: the noindex rules in _headers were written with the OLD project name hardcoded, so moving project would have silently made the preview indexable. They are now written with placeholders (:project.pages.dev and :version.:project.pages.dev), so any future move is covered. THE PREVIEW IS NOW https://pd-review.pages.dev and the seven live files plus the README deploy command were repointed at it. The dead project stays for a day in case Cloudflare unsticks it, then gets deleted (NOW).

- **2026-08-26 · Session close and continuity handover.** The evening session grew long enough to hurt, so it was closed cleanly for a fresh one to continue the same day. Filed: HANDOFF.md rewritten whole (the preview URL change is the first thing it says, since the old one is dead and a next session would otherwise verify against a frozen page), NOW.md carrying the dead-project deletion and the go-live name, STATE.md, accounts.md, separation.md and landing-page.md repointed from pro-debouchage-v3 to pd-review, and the README deploy command with it. Also removed one piece of dead code the move exposed: build.js still declared PREVIEW_HOST for the old host, unused since the noindex rules went to placeholders. New rule in AGENTS section 10 on Fady's own words: he reacts to the work, not to explanations, so ship it, hand him the link and a few lines, and put anything that is genuinely his decision in a widget. Final state verified: build clean apart from the go-live placeholder warning, no BOM, all eleven pages plus sitemap and robots 200 on the live preview, custom 404 answering 404, noindex present, project root tidy, every file inside its cap.

- **2026-08-26 · Afternoon, ads strategy research (Claude Code, Fable 5, five parallel agents: three Opus, two Fable).** Ran read-only beside the design session by agreement, output confined to research/. Filed research/24, the merged ads research: 2026 platform changes that break parts of ads-program.md as written (AI Max on by default in new campaigns, the language setting removed from Search late September, call-only ads dead since February, bid adjustments ignored under Smart Bidding, so the night and device modifiers are dead letters), the niche consensus playbook, the Belgian advertiser and price landscape (hb-debouchage.be confirmed advertising, no Dutch; lead resellers prop up head-term CPCs), Belgian-authentic FR and NL keyword tiers with negatives, and 11 recommendations including one campaign with two language ad groups and proposed answers to research/14's seven open questions. Decided and filed: ads do not wait on the Business Profile (was already the plan, reconfirmed); the billing card deviation (DECISIONS). ads-program.md deliberately NOT touched: the decisions land at the dedicated ads session with Fady.

- **2026-08-26 · Late evening, continuation session: the section pass ran from the photo story to the who band, and one recorded fact turned out to be wrong.** (1) SPACING. The gap between services and the photo story was 164px of dead white on desktop and 88px on a phone, because both bands are `s-card` and their two section paddings stacked with no colour change to mark a boundary; the band now carries no padding of its own and the section above supplies the whole separation (82px / 44px). The give-back BELOW the band was a fixed 68px, but the photos overhang by a share of their WIDTH, so at 880px the next heading was 4px from touching a photo while at 1280px it floated 93px away: it is a percentage now and tracks at every width. Also found and fixed a real defect on the way, the enlarged Afrem photo pushed the document 6px wider than the screen at 880px and gave a sideways scrollbar. (2) THE BOWL RULE WAS WRONG, and this is the entry worth re-reading. `styles.css` recorded that the blocked bowl sits at "19 to 38 percent across, 42 to 51 percent down" of the before photo, and every automated clearance check that day asserted against that rectangle and passed. Opening the image showed that rectangle is the SEAT AND LID: the dirty water is at about 34 to 64 percent across and 71 to 90 percent down, the bottom right corner, which is exactly where the after photo had drifted, so the subject that sells the section sat crowded while the checks reported it clear. The note predates the before-photo swap of the previous evening and was never re-checked. Corrected in `styles.css` with an instruction to verify against the image and never against the comment, and the same lesson written into memory beside "view images before picking". Fady saw it on his phone before any check did. (3) The three drawn frames left the Figma-sampled brights for the page's own tokens: `--ink` before, `--teal` during, `--cta` (the call button's red) after, on Fady's ask. His first choice for the before frame was the page yellow; measured against the near-white card it comes out at 1.38:1 where teal reads 5.09 and red 4.58, so it would have been a faint wash beside two solid lines, and he picked navy from a widget carrying those numbers. Mobile composition then went through four rounds on his notes: photos apart to breathe, Afrem and after enlarged, the before dropped and nudged, a 42px gap opened under the heading, everything kept inside the screen at 375, 414, 640 and 880.
- **2026-08-26 · Same session, three new pieces of the page.** (1) THE CITED FACT. The scam band's "plus de 265 victimes entre 2020 et 2025, et un procès en 2025" is now a button carrying the yellow marker and a small i; it opens a real dialog ON the page with the prosecutor's date (29 September 2025, 17 defendants, Halle-Vilvoorde judicial district), what the file covers, and the sources named (om-mp.be press release, VRT NWS, RTBF, and the Moustique investigation of 17 October 2025). Escape, backdrop and button all close it, focus moves to the close button and returns to the phrase, and there is deliberately NO outbound link: the whole point is to prove the claim without losing the visitor. The claim was checked against research/18 before a word was written, and it holds. (2) WHO COMES TO YOUR DOOR was rebuilt: Fady could not tell what the section was on a wide screen, because the old two-column split put the van beside three unlabelled text blocks. It now reads top to bottom, the paragraph, then Afrem speaking in a chat bubble, then the van under him, and the three rules that used to be that column became their own section, "Trois choses sur lesquelles on ne bouge pas", as three cards with drawn icons in teal, navy and red, reusing the frame trio so the page keeps ONE colour system. (3) PRICES: a permanent yellow marker under every amount and the surcharge text pushed off the last row (16px to 30px). The one-by-one reveal Fady asked for was built, staggered properly, and then REMOVED at his call, he could not catch it firing; the marker is simply there from first paint, and the CSS says that is a decision and not an omission. A specificity bug was caught building it, `.js .rv` outranks `.pricelist li`, so the rows were stepping at 50ms while their markers stepped at 110ms and would have drifted apart.
- **2026-08-26 · Same session, the honesty call and the close.** Fady asked for an AI portrait of Afrem for the bubble, and Roro has been asked for a face photo but has not sent one. No face was invented: putting a generated face on a real, named colleague is exactly what rule 5 forbids and exactly the kind of thing that ships quietly, so the avatar is a monogram inside the brand gradient ring and `build.js` now warns about it on every run, next to the placeholder-reviews warning. The moment Roro's photo lands, the branded portrait gets made from HIS OWN face, the same way the taxi driver portraits are made, and the bubble is already built for it (small, rounded, gradient-ringed, which also answers Afrem being shy). Also this session: the services H2 became "Votre problème est dans cette liste." in all three languages on Fady's exact words, superseding the "Pas de panique" line picked earlier the same day; the steps section was left untouched at his explicit request; and the note under the review cards, which had `margin-top: 0` and sat flush against them, got 20px on phone and 30px on desktop. Closing health pass on the deployed preview: eleven pages build, the source dialog opens and closes in FR, NL and EN with the right heading in each, three cards and the bubble present in all three languages with the line written natively per language, no console errors, no BOM on `styles.css`, no sideways scroll at 375, 640, 880 or 1280 on the home pages or the legal pages, and both tripwires firing. Roughly a dozen deploys this session, every round verified on the deployed URL by DOM measurement, never in the local build.

- **2026-08-26 · Night, the ads decision round (Claude Code, Fable 5, same session as the research, no agents).** Fady, tired of design but awake, chose to close the ads strategy instead of a third design round. Eight decisions taken by widget (DECISIONS 2026-08-26 night) and `playbook/ads-program.md` rewritten in place to the decided spec: one campaign two ad groups, emergency-only launch keywords, Maximize Conversions day one with fallback, 24/7 flat no modifiers, no EN ads, promise-not-numbers copy, /fr/ and /nl/ URLs at launch, no commune negatives. Pre-flight gates extended: Keyword Planner CPC pull, verification pack, EU political declaration, the temporary-card billing line. Conversion threshold on forwarded calls raised 45 to 60 seconds per research/24. The ads program is now fully decided up to the account session; what remains there is execution (account, billing, tag proof, build, pause, enable on Fady's word).

- **2026-08-27 · Night, THE GO-LIVE PUSH (Claude Code, Fable 5, overnight while Fady sleeps).** Fady's call before bed: push v1 live as is, prepare everything, design continues in the morning. Done and verified: Paolo's real review shipped as a new featured single-review card (design decided by Fady, built tonight: teal top bar, oversized quote mark, no stars, translations marked; placeholder cards deleted, 3-card grid parked, build tripwire retired); rebuilt, new Pages project `prodebouchage24` created, deployed; domains prodebouchage24.be and www attached (CNAMEs added in the Cloudflare dashboard through Fady's Chrome, per his authorization); SSL raised Full to Full (strict). Live verification: 12 of 12 pages 200 on the apex, custom 404, security headers served, apex indexable while pages.dev keeps noindex, canonical correct, featured card confirmed rendering on desktop and clean at 375 (a scare about an invisible card was the reveal animation's timing, disproved by DOM inspection: class in, opacity 1). Filed: STATE, NOW, accounts, landing-page, README, two DECISIONS entries. Notable: G4 satisfied; the old .com's fake content is now the one live rule-1 violation, blocked on unknown logins (NOW, clock). Earlier the same evening the app closed by itself once (cause unknown, session resumed cleanly). Still owed tonight: the invoice Word template.

- **2026-08-27 · Deep night, the everything-else sweep (Claude Code, Fable 5; four Opus agents, Fady awake and authorizing).** Filed research/25 (the paste-ready Ads account build sheet: one campaign, two ad groups, counted RSAs with the Vilvoorde rule enforced, merged negatives, 14-step session checklist) and research/26 (Belgian invoice legal musts with the 6 percent sentence verbatim FR and NL). Legal audit of the live site ran: passed everywhere except the Cloudflare e-mail obfuscation (turned OFF in the dashboard, proven fixed on the wire) and an SRL/BV slip on /nl/privacy (fixed, deployed, verified). GA4 CREATED in Roro's login by an agent in the business Chrome on Fady's explicit go (G-S3SQ25WZMK, property 551825707, enhanced measurement off, hi@fady.be admin) and Search Console domain-verified with the sitemap read, Success, 9 pages (accounts.md updated). The daily "clients du jour" Tally form built and live (tally.so/r/PdJkkB). The invoice pack built: facture + bon d'intervention (closing the CGV work-order gap) + printed CGV in one branded .docx with a simple-English README (assets/prepared/invoice/). VIES polled twice, still INVALID. Open from tonight: Fady eyeballs the .docx in Word, deletes pd-review and pro-debouchage-v3 when a day has passed, accountant's four VAT-rate questions.

- **2026-08-27 · Day, the ads account session (Claude Code orchestrator, Fable 5 in-session with two Opus agents; NOTE: execution belongs on Opus agents, corrected going forward).** The .com takedown prepped (research/27, drafts ready) then PARKED until after ads day 1 (DECISIONS); depot visit pack built and sent for Friday. The Ads account went from nothing to campaign-ready in Roro's login with Fady's clicks at the gates: account 166-502-9105, billing on the Workspace profile with Fady's card, 400-for-400 credit (spend deadline 26 Oct), auto-apply all off, EU political NO, Keyword Planner CPC gate PASSED (research/28), three conversion actions with tag AW-18413234511 and labels filed (accounts.md), negative list "PD | Negatives | shared" 242 keywords, campaign PD | Search | Ring Bruxelles | FR+NL fully drafted (106 towns + Brussels excluded Presence, all languages, AI Max off, FR ad group 15 keywords + unpinned RSA 15/4, budget 20) to "ready to publish". Blockers fought: Google's "Confirm it's you" gate silently discarded a full round of keyword/ad/budget entries and refused Fady twice, solved by signing info@ into Fady's phone; advertiser verification started by Fady with the legal name entered as "Pro Debouchage" (correction to PRO DEBOUCHAGE SRL owed). Publish + pause, NL ad group, assets and read-back hand over to the next session; design session ran in parallel and logs its own entry.

- **2026-08-27 · Evening, THE CAMPAIGN IS PUBLISHED AND PAUSED (Claude Code, Fable 5 driving the business Chrome; classifier refused the agent handoff, Fady's go given in chat).** The draft's saved budget had been lost: re-entered (custom 20 euro/day, Google's 46,29 suggestion refused), published (new live campaign id 24185896982; the draft 10211133666 is spent), PAUSED within seconds, confirmed Paused plus "Account paused" for advertiser verification on top. Ad group 1 renamed FR emergency; NL emergency BUILT to research/25 (15 NL keywords exact/phrase, unpinned RSA 15/15 + 4/4 NL native, paths ontstopping/dringend, final URL /nl/ after Google prefilled /fr/ and 9 FRENCH auto-generated headlines, all replaced); one Google identity check passed silently. Assets per section 6: FR+NL callouts (4+4), FR+NL Services snippets (6+6), FR+NL sitelinks (4+4), all ad-group level; call asset 0480 649 649 exists account-level, Eligible. Negative list PD | Negatives | shared ATTACHED at campaign level. Read-back: Paused, Search only, Maximize conversions no target, 20/day, 106 locations + Brussels province excluded, Presence-only, All languages, AI Max off, ACA off, broad-match off, goals call_click (Contacts), 19 asset associations. FLAGS: FR snippet disapproved "Tobacco" (absurd, appeal when unpaused), ads/sitelinks Under review, hi@fady.be Admin invite already ACCEPTED (Users list, signed in today), advertiser-verification disclosure name "Pro Débouchage" is correct as-is; the SRL legal-name match happens in the pending Dun & Bradstreet task (Fady, KBO pack ready).

- **2026-08-27 · Evening-night, verification passed, G5 done, the two-tree collision (Claude Code orchestrator, Fable 5; execution on Opus agents plus in-session browser for the appeal).** Advertiser verification PASSED instantly on Fady's D&B submit (SRL legal name, seat address); "Account paused" lifted; the Tobacco flag on the FR snippet APPEALED (Dispute decision, "Appeal accepted", NL snippet Eligible). G5 tag round DONE by an Opus agent: flag-on build deployed, wire proof passed on the live apex (zero Google pre-consent FR+NL, conversions fire with the right labels), then the four tag ids baked into build.js as DEFAULTS (TAGS_OFF=1 for the clean build). research/29 cookie-banner compliance filed by a second Opus agent; B2 decided on Fady's delegation: keep GA4, purpose-split second layer (DECISIONS). The day's incident: the design worktree and the main checkout overwrote each other's deploys (minutes of tagless live site, broken legal footers from an unanchored docShell); reconciled the same evening, worktree copied over main, READMEs hand-merged, both trees byte-identical to the live deploy, confirmed independently by both sessions. Banner round handed to the NEXT design session (the old one closed).
- **2026-08-27 · Design session, the cookie banner round (Claude Code, Opus 5, evening, no agents, working directly in `design/site-source/`, sole deployer).** Fady settled the day in a QA widget first: banner and privacy work first, then he reviews the whole site with fresh eyes and sends all his notes in one go, and the session makes NO section edits of its own until then (the old "section pass" mandate from earlier sessions is discarded). Built research/29 B1 to B9 on the live site: the banner's first layer now carries the controller, the withdrawal sentence and a link to the privacy page (B1); a second layer splits the choice by purpose with two switches, audience measurement and ad measurement, both off on a first visit and pre-set to the stored answer on a reopen (B2, B8); `pd_consent` became `{ads, analytics, v, t}` and `CONSENT_VER` was bumped to `2026-08-27-purposes`, so every one-button record is invalidated and the question is asked again. Privacy pages FR/NL/EN: Google Ireland Ltd named in the recipients list (B3), the four-row per-cookie table with names, purposes, "jusqu'à" durations and recipients including `pd_consent` as explicitly not a cookie (B4), the outside-the-EU transfer paragraph (B5), the date moved to 27 August 2026 (B6), and consent added to the legal-basis list (B7). Two new tag-day markers (`<!--recip-->`, `<!--basis-->`) join the existing cookies swap, and `build.js` now throws if any marker survives, so a `TAGS_OFF=1` build can never claim a recipient or a basis it does not have. The card was redesigned to read as part of the site (paper card, the radius ladder, the section's yellow rule, the weight ladder) while the two buttons stay ONE CSS rule so Refuse and Accept cannot drift apart; "Choisir" / "Zelf kiezen" / "Choose" is quieter but full weight, full contrast and 44px, and it ADDS a layer rather than replacing Refuse.
  THREE THINGS THE VERIFICATION CAUGHT, all fixed and re-deployed. (1) `enable()` guarded the whole function on `loaded`, so a visitor who accepted audience measurement and then also accepted ad measurement got a stored `{ads:true}` while gtag stayed denied. It now handles upgrades and downgrades inside one page view, sending a `denied` update and wiping the matching cookies when a purpose is turned back off. (2) Cloudflare caches `/assets/js/consent.js` for an hour at the edge and four in the browser, so the first deploy served the OLD card under the new HTML; the script URL now ends in a content hash of the generated file, which busts on any change and is independent of the storage version. (3) Layer two pushed "Retour" below the card's own fold on a 375x812 phone; the repeated fine print was cut to the bare link there and the rows tightened, so both layers now fit with no internal scroll.
  Verified on the live apex, not a preview: zero third-party hosts and zero Google scripts before the choice with only the denied defaults in the dataLayer; Refuse and Accept identical to the pixel (194x52 desktop, 146x52 at 375, same fill, border, weight and size); analytics-only grants `analytics_storage` alone, loads only the GA4 loader and sets only `_ga*`; the upgrade to ads adds `ad_storage` + `ad_user_data`, configures AW-18413234511 and sets `_gcl_au`; the call CTA fires the conversion with the right label only while ads are granted and stops when they are withdrawn; Refuse wipes the cookies and returns focus to the footer link; `ad_personalization` is granted in no branch. Privacy table scrolls inside its own box at 375 with no page overflow, all three languages complete, no console errors. STILL OWED, and they are Fady's clicks, not code (research/29 B10): set GA4 data retention to 2 months instead of 14, and confirm the Ads and GA4 accounts sit on Google Ireland Ltd terms with the data processing terms accepted.
- **2026-08-27 · Design session, Fady's first fresh-eyes round, six comments (Claude Code, Opus 5, evening, no agents, same session as the cookie round above).** (1) The mobile carousel hint lost its words: the arrow-shaped chip reading "Glissez pour voir" became a drawn hand that swipes inside a round ink container, animated in CSS and dead under prefers-reduced-motion. A gesture reads the same in FR, NL and EN, and the strip already carries a labelled role=group for anyone who cannot see it; the `swipe` key is gone from all three copy files. (2) The service rows' hover is now the house highlighter: a yellow line that slides in under the TITLE, painted with background-size so it follows the words instead of underlining the whole grid column, replacing a 2px lift Fady could not see. On a touchscreen the lines are simply there from the start, his call, because a phone has no hover. (3) The handwritten note stopped being a stopwatch and became the customer's reaction: "Ah..." over "parfait !" in FR, "perfect!" in NL and EN. That needed a NEW CAVEAT SUBSET (the old one held 15 glyphs and none of A, h, p, f, c, "." or "!"): re-fetched from Google Fonts for the exact 13 characters the three strings need, 7,316 bytes, with `NOTE_GLYPHS` in build.js and the `unicode-range` in styles.css updated together so the guard still fails loudly on an unsubsetted letter. On a phone the note dropped out of the collage and now straddles the seam between the white section and the grey one below it, 68 percent on the white side, moved right from x=34 to x=64 and still clear of the third photo. (4) The cited "265 victimes" fact reads as a sentence again: it was a `<button>`, and Blink coerces display:inline on a button to inline-block, so the whole clause became one atomic 335px box that could not wrap and dropped onto a line of its own. It is a `<span role="button" tabindex="0">` now, inline for real, with Enter and Space added back in PAGE_JS; the round "i" still rides the last word. (5) The van caption is one short line, and on wide screens WHO COMES is two columns with the words and Afrem's bubble left and the photo right. (6) 46px of top padding on the featured review, so the big teal quote clears the first line.
  TWO THINGS WORTH KEEPING. The van caption carried the "image recréée" disclosure and Fady asked for it gone; the honesty did not disappear with it, it moved into the footer's `photoNote`, which already carried the same notice for the before photo (rule 1). And measuring anything inside `.ba-collage` in a hidden preview pane reads 10px low, because the `.rv` reveal holds it at translateY(10px) and the observer never fires there: the first note position measured 84/16 and looked like 67/33 until the transform was neutralised inline. That trap is now written into styles.css beside the rule.
  Verified on the live apex at 375 and 1440, FR, NL and EN: hand animating with no text and the carousel still hidden on desktop, marker at 100 percent on touch and 0 percent idle on a pointer while hugging the words (326px against a 467px column), note straddling at 68 percent with Caveat really loaded in all three languages, the cited clause flowing mid-line across three lines with the icon bound to the last word, who-grid at 488px of words against 572px of photo on the same row, 15px between the quote's ink and the first line of the review (canvas ink metrics, not box metrics), no page overflow anywhere, no console errors. OPEN FOR THE NEXT ORCH SESSION, on Fady's instruction, to be cleared before the readiness audit and before ads are enabled: GA4 data retention to 2 months instead of 14, and confirm the Ads and GA4 accounts sit on Google Ireland Ltd terms with the data processing terms accepted (research/29 B10).
- **2026-08-27 · Design session, Fady's second round, three changes (Claude Code, Opus 5, evening, same session).** (1) The swipe gesture lost its navy pill: the bare hand now sits INSIDE the first photo, 20px in from its left and bottom edge, in white ink with a drop-shadow so it survives whichever job photo is under it, and it leaves a trail of three dots behind the finger as it travels. The dots are real circles on staggered delays, not a dashed stroke, because each has to arrive on its own beat as the finger clears it and a dash pattern can only slide. Under reduced motion the hand rests at the end of its travel with the whole trail already behind it, which still reads as "this slides". (2) The handwritten note is ONE WORD with no underline: "Parfait !" in French, "Perfect!" in Dutch and English. The yellow swoosh, the second line and the `.ba-note-u` SVG are gone, `baNote` went from a two-item array to a string (the build guard follows), and the Caveat subset was re-cut a second time to the ten glyphs those two strings need, 6,400 bytes. Cutting the note in half lifted it clear of the section seam entirely, so the phone anchor went from 92 to 96.4 percent to put it back across the white-to-grey edge at the same 69 percent it had before; that top value is tuned to the note's HEIGHT and is now commented as such, because it moves every time the wording does. (3) The consent card's buttons, on his call: Refuse and Accept are filled navy hovering to the house yellow, and "Choisir" became a real ghost button in the same shape rather than an underlined word. The white-on-white pair did not read as pressable. Refuse and Accept still share ONE selector so they cannot drift apart, and the ghost is legal precisely because it is the third control: it opens the per-purpose layer, it never stands in for the reject button (research/29 A4 and B11, APD v Mediahuis September 2024).
  Verified on the live apex at 375 and 1440 in FR, NL and EN: hand with no container at a measured 20px inset inside the photo with three animating dots, note one line with no underline straddling at 69 percent and Caveat really loaded in all three languages, both banner layers showing Refuse and Accept byte-identical in fill, border, radius, weight and size (146x52 at 375, 194x52 at 1440) with one yellow hover language across the card, layer two still fitting a 375x812 phone with no internal scroll and 86px of clearance above the call bar, carousel still hidden on desktop, no page overflow, no console errors.
- **2026-08-27 · Design session, Fady's third round (Claude Code, Opus 5, evening, same session).** (1) The swipe gesture's trail dots turned yellow, and the hint now RETIRES: one passive scroll listener on the carousel fades it away for the visit once the third photo is reached, and removes itself. A hint that never leaves is furniture; by the third slide the gesture has been understood. It is a no-op on desktop, where the carousel is display:none and never scrolls. (2) The handwritten note moved left (x 67 to 44), down a touch and a size smaller (25.9px to 23.25px at 375), so 14px of "Parfait !" now hangs into the grey band instead of 11. (3) The consent card was rebuilt around one button family and one spacing rhythm on Fady's call: every control, the two answers AND the ghost, is now the SAME button (one shared rule for height, padding, radius, border weight, type size and hover), only the fill separates a decision from the ghost; the ghost runs the full card width under the answers so the hierarchy reads two answers, then the quieter way in, then the fine print. Spacing is 12 / 14 / 16 / 10 / 18 on both layers, nothing spaced by feel. On the choices screen the second "Tout refuser" is gone and "Enregistrer mes choix" is a full-width primary with "Retour" as a full-width ghost under it. THAT IS SAFE and the reason is written into build.js: the reject button the APD requires must sit on the FIRST layer beside accept at equal prominence, and it does; both switches are off until the visitor turns them on, so Save with nothing ticked IS a refusal and runs the same wipe, verified on the live page. research/29 B2's second Refuse here was a preference, not the requirement.
  ONE REAL BUG, ONE PROCESS BUG. The fine print's top margin had NEVER applied: `.c-fine` is a lone class and the card's own `.consent p` (one class plus one type) outranked it, so the sentence under the ghost sat flush against it with a stray 16px underneath instead. That is exactly the gap Fady's eye caught. Fixed by scoping the selector; it now sits 18px under the ghost and flush with the card's bottom padding on both layers. Separately, a CSS comment written with backticks inside build.js's template literal broke the build, and because the command was `node build.js | tail`, the shell reported the pipe's exit code and the deploy ran anyway on the previous output. No harm (site-v1 still held the last good build, so the live site never broke) but the lesson is real: NEVER pipe the build into anything in a chain that then deploys, or check PIPESTATUS.
  Verified on the live apex at 375 and 1440 in FR, NL and EN: dots #FFD635, hint gone at slide three and still present at slide two, note at 14px of overhang in all three languages, both banner layers measuring 12/14/16/10/18 with the ghost matching the row width to the pixel (301px at 375, 398px at 1440) and the answers still identical to each other (146x52 and 194x52), layer two fitting a 375x812 phone with no internal scroll and 86px above the call bar, Back returning to a layer one that still carries Refuse, and Save with nothing ticked storing a refusal with zero Google scripts. No page overflow, no console errors.
- **2026-08-27 · Design session, Fady's fourth round, the consent card again (Claude Code, Opus 5, evening, same session).** Trail dots back to white, his call, one line reverted. Everything else was the card. (1) A DIM BEHIND IT, light on the first layer and deeper on the choices layer, because by then the visitor is deciding rather than glancing. It is `pointer-events:none`, which is the whole reason it is allowed: the page underneath stays fully usable, so it weights the card visually and is never a cookie wall (research/29 A7). It sits at z-index 190, UNDER the sticky call bar at 200, so the phone number stays bright and tappable behind it, which B9 asks for by name. (2) PHONES GET YELLOW ANSWERS (below the card's own 1000px breakpoint, the one that already moves it into the corner on desktop); desktop keeps navy. Both answers are still one selector, so they change together and cannot differ. Their hover flips to ink so a tap that leaves a stuck hover still reads as a press. (3) The ghost now speaks the site's own ghost-CTA language (`.btn-ghost`): ink outline, ink text, and it FILLS WITH INK on hover instead of going yellow. It keeps the card's 52px rather than the site's 64px, so it stays in the same family as the two answers, which is what round 3 asked for; the full 64px version is one line away if Fady wants the CTA inherited literally. (4) A MOVING STROKE around the card, his test: a conic gradient built from the logo file's own hex values (its aqua #25FEC0 and #22B3A6, its blues #2E6FB1 and #0F4A6A, its yellow #FFDC52, its orange #FE7740), one slow turn every 18 seconds.
  THE STROKE IS BUILT TO COST NOTHING, and the choice of technique is the whole point. It is an 820px conic-gradient square that is ONLY ROTATED, which Chrome runs on the compositor: the GPU spins one existing layer and the main thread never repaints. The two obvious alternatives both repaint the whole card every frame, an animated gradient angle via `@property` and an SVG dash offset, and were rejected for that. The card clips it with `overflow:hidden` and the body sits on top leaving 2px showing, so the scroll had to move from `.consent` to a new `.c-body` wrapper: the frame clips, the body scrolls. It stops under `prefers-reduced-motion`, and it only exists while the card is open, so it is not a standing cost on the page.
  Verified on the live apex at 375x812, 375x600 and 1440x900 in FR, NL and EN: dots white again, dim 0.34 on layer one and 0.6 on layer two with pointer-events none and a hit-test through it returning the page's own H2, dim below the call bar and removed with the card, ring conic and 18s linear leaving an even 2px on all four sides, yellow answers on phone and navy on desktop with the pair identical to each other in both, ghost transparent with an ink-fill hover at the same 52px as its siblings, and on a deliberately short 600px viewport the body scrolls internally while the ring stays put and the fine print stays reachable. No page overflow, no console errors. (Reading a transition mid-flight in the hidden preview pane returns the START value, so the deeper dim had to be confirmed by killing the transition inline; same family of trap as the reveal transform.)
- **2026-08-27 · Design session, Fady's fifth round, the consent card (Claude Code, Opus 5, evening, same session).** Yellow answers on phones lasted one round: he prefers the navy at every width, so the `max-width:999px` override is gone and the comment where it stood now says not to reintroduce a width-dependent fill without keeping both answers on the one shared selector. The card gained the logo above its title, small (26px, against the site header's 34 and the footer's 44, because here it is a signature and not a navigation target) and left-aligned to the same edge as the text, with the title up from 18 to 20px. That is what he asked it to do: on brand, and 42px taller on the first layer so it covers a little more of the page. The mark is decorative, alt="", since the dialog already carries its own label.
  Verified on the live apex at 375x812, 375x667 and 1440x900 in FR, NL and EN: logo loaded and left-aligned to the title's own left edge with a 12px gap under it, title 20px, both answers navy and identical in fill and width (193px at 1440), first layer 496px tall on a 375x812 phone against 454 before, and on the deliberately short 375x667 the choices layer caps and scrolls inside the body while the moving stroke stays put around the frame at an even 2px and the fine print stays inside the card. No page overflow, no console errors.
- **2026-08-27 · Design session, Fady's sixth round, the choices layer (Claude Code, Opus 5, evening, same session).** Three fixes and one non-fix. (1) The logo is now on the FIRST layer only: it is a greeting, and on the decision screen it was 38px of height doing nothing. (2) The choices layer CENTRES itself on a phone (`.c-mid`, below the card's own 1000px breakpoint). Layer one stays a sheet at the bottom near the thumb because it is a glance; layer two is the tallest thing the card ever shows and, bottom-anchored, it ran from under the status bar to just above the call bar and read as a page rather than a dialog. Centred it is a dialog again, and it leaves the bottom of the screen free, which keeps the call bar clear as well. Desktop keeps its corner, where the card is small and the corner is the point. (3) The card's bottom padding is now larger than its top (26 against 20) because the card ends in 13px fine print, which needs more room under it than a symmetric block gives before it reads as sitting on the edge. THE NON-FIX: he asked for the choices button to be navy rather than yellow, and it already was. Round 5 had removed the phone-only yellow fill; the only yellow left on that screen is the 44px rule under the title. He was looking at the round 4 build, which is a caching lesson worth keeping: after a deploy, a phone that has the page open keeps the old inlined CSS until a real reload.
  Verified on the live apex in FR, NL and EN: at 375x812 the choices layer is 634px tall against 666 before, exactly centred (89px above, 89 below), no logo, save button navy, moving stroke even at 2px on every side, fine print 28px off the card's bottom edge; at 375x667 it caps, centres and scrolls inside the body with the stroke staying put and the fine print still inside the card; at 1440x900 both layers stay in the corner at 24px with the logo on layer one only. No page overflow, no console errors.
- **2026-08-27 · Design session, Fady's seventh round: the yellow button explained, and the H1 (Claude Code, Opus 5, evening, same session).** THE YELLOW BUTTON WAS A HOVER, and both halves of what he reported prove it: the resting fill had been navy since round 5, the only yellow rule left anywhere near those buttons was `:hover`, and a browser does not re-evaluate hover when innerHTML is replaced until the pointer moves again, which is exactly why "it turns navy when I toggle choices on and off". Fixed properly rather than argued: hover is now `--ink-press` (#0A1D33, a darker navy, following the convention the red CTA already uses with --cta / --cta-press), and EVERY hover rule in the card is wrapped in `@media (hover:hover)`, so a touchscreen can never keep a colour stuck on whatever was last tapped. `:focus-visible` stays outside the query because keyboard focus is real on any device, and `:active` gives the press its own feedback. Verified on the live page: no `--mark` left on any consent button, both hover rules gated, resting fill navy.
  THE THREE H1 CAPS, re-measured. Fady: "the French headline is perfect, the other two break badly." Measured at 375 on the live page, the defect was not the size but the WRAP: the highlighted answer line broke one word early and stranded a 51px "it." and a 55px "op." alone on the last line. English went from 38.5 to 43.7px (cap 11.5 to 13.05cqw) and Dutch from 33.2 to 38.2px (9.9 to 11.4cqw), both landing at 93% of their column against French's 87%, which forces the break one word earlier and balances it: EN 240/166, NL 286/135. Each cap is the largest size at which that language's LONGEST hard-broken line still fits, which is why they differ so much (French's longest is 11 characters, Dutch's is 16). Desktop is untouched, all three hit the 108px --t-display ceiling there. THEN THE BREAKS WERE LOCKED: desktop English still orphaned "it." under a full-width line, so every h1b now joins its word groups with NO-BREAK SPACES, leaving exactly one ordinary space as the only place the line may break (FR "Appelez. On | s'en occupe.", NL "Bel. Wij lossen | het op.", EN "Call. We'll | sort it."). Desktop EN went from 1089/173 to 744/518 and French, already good, improved to 802/855. Nothing overflows at 375 or 1440 in any language.
  Also: the English tip line said "send a photo, we'll see it right away", which reads as a promise about how fast we LOOK. That is not the point and it is not what the French and Dutch lines say. Now "one photo and we already know what we're dealing with".
- **2026-08-27 · Design session, the hero pill (Claude Code, Opus 5, evening, same session).** The eyebrow above the H1 stopped being a bare grey line and became a pill wearing the same slow moving stroke as the consent card, built identically and for the same reason: a conic gradient square that is ONLY ROTATED, so the compositor spins one layer and nothing repaints. 22 seconds a turn, slower than the banner's 18 because this one lives on the page for the whole visit rather than until a click; it stops under prefers-reduced-motion. Copy is Fady's own pick from a widget, two words, what we do and when: FR "Débouchage 24h/24", NL "Ontstopping 24/7", EN "Drain unblocking 24/7", replacing "Débouchage pro et honnête · 24h/24" and its two counterparts. He also picked the QUIET version, and it needed one correction after measuring: filled with `--paper` the pill is invisible inside its own stroke, because --paper IS the page; `--card`, one shade lighter, makes it read as an object. The loud navy version is one declaration away and the comment says which.
  Verified on the live apex at 375 and 1440 in FR, NL and EN: one line in all three (196, 179 and 221px wide, 36 tall), an even 2px of stroke on every side, ring conic and animating, clipped by the pill, left edge flush with the H1's, 26px of air under it, no page overflow, no console errors.
- **2026-08-27 · Design session, the pill rebuilt and the English tip measured (Claude Code, Opus 5, evening, same session).** THE PILL'S THREE FAULTS WERE ALL REAL, and all three were Fady's eye finding something a measurement then explained. (1) The "colours cut out when it moves" was the conic gradient: its centre is a singularity and the pill is only 30px tall, so the top and bottom of the stroke sit some 15px from that centre where the angular sectors are a few pixels wide and the colours band. A conic gradient is simply the wrong tool for a wide flat shape. It is a LINEAR gradient sliding sideways now, no centre at all, still a transform-only animation and still compositor work with no repaint. The colour order is also a PALINDROME so the strip tiles into itself with no seam, and the only bright-to-bright jump left is aqua to yellow, which passes through a clean light green; the previous loop had a blue-to-yellow crossing that goes through grey, which was the other half of what looked cut. (2) The "white happening" was the fill: --card is the cream the cards use and against --paper, the page, it read as a white plate behind the words. The fill is the page colour now, so the pill is its stroke and its words and nothing else, which is what he asked for. (3) The extra space is back down: padding 8 to 4, margin 18 to 12, so the pill is 30px tall instead of 36 and the top of the pill to the top of the H1 went from 62px to 50.
  THE ENGLISH TIP LINE was measured rather than guessed, and Fady's own shorter version does not fit: at the hero's font the column is the viewport minus 40, and "one photo and we know what we're dealing with" is 348px against 335 available, so it wrapped by 13px (the previous line was 395px and wrapped badly). It now reads "a photo, and we already see the problem", 303px, one line down to a 343px screen, which is narrower than the French line itself manages (353px), and it is the closest English to what the French and Dutch lines actually say.
  Verified on the live apex at 360, 375 and 1440 in FR, NL and EN: pill 30px tall in all three (190, 173 and 215 wide), fill identical to the page background, gradient linear with no conic left anywhere except the consent card, strip exactly twice the pill width so the loop closes on itself, even 2px of stroke, left edge flush with the H1, English tip on one line at 360 as well as 375. No page overflow, no console errors.
- **2026-08-27 · Design session, the text version of the hero label, shipped beside the pill to compare (Claude Code, Opus 5, evening, same session).** BOTH ARE ON THE PAGE RIGHT NOW, pill first and text second, so Fady can judge them on the page rather than in his head. THIS IS TEMPORARY: one of the two comes out the moment he picks, and the page must never reach a customer carrying both. The duplicate is aria-hidden meanwhile so a screen reader is not told the same thing twice.
  The text version puts the moving colour IN the letters with background-clip:text, so there is no plate, no stroke and no added height at all: 18px of ink against the pill's 30px box, and from the label's top to the H1 it is 38px against the pill's 50. TWO DECISIONS INSIDE IT ARE NOT TASTE. First, the colours are the logo's DARK register only, because this is 13px text on the paper: measured against #F6F3EE, #0F4A6A clears about 8.9:1, #AC3E32 about 7.0, #126A75 about 6.4, #2E6FB1 about 4.7 and #CE6339 sits on the 4.5 line, while the bright aqua #25FEC0 and the yellow #FFDC52 that the pill's stroke uses come in around 2.5:1 and would be close to invisible. The gradient still sweeps the logo's cool-to-warm arc, in its darker half, and it is palindromic like the pill's so the loop closes on itself. Second, weight 800 rather than the pill's 600, because more ink is what makes a gradient legible at this size. Unlike the two rotating strokes this one DOES repaint each frame, since background-clip:text cannot be moved with a transform; on roughly 200 by 18 pixels that is nothing, and the comment says why the technique is not used on anything bigger. A plain ink colour is set before the @supports block, because without background-clip:text a transparent colour would erase the words.
  Verified on the live apex at 375 and 1440 in FR, NL and EN: both versions rendering and left-aligned to each other and to the H1, clip active and the gradient applied in all three, 14s loop, no page overflow, no console errors.
- **2026-08-27 · Design session, the hero label settled (Claude Code, Opus 5, evening, same session).** THE PILL WON. The text-only version it was compared against, moving colour inside the letters via background-clip:text, is deleted whole from the template and the stylesheet; it measured well (18px of ink against the pill's 30px box) but lost on looks, and the comparison state where both shipped together is closed.
  THE STROKE'S COLOUR ORDER, third and final. Fady: "the colors look duplicated and the movement is duplicated". Both were the palindrome. Mirroring the ramp was how the previous build closed the loop without a seam, but a palindrome shows every colour TWICE per pass and reads as bouncing rather than travelling, which is exactly what he saw. It is a true one-way cycle now: deep blue, teal, aqua, blue, back to deep blue. FOUR colours, each appearing once, every step a hue neighbour, so it closes on itself with no seam and no mirror: dark to light around the cool arc, then darkening back through the mid blue. The logo's warm half is deliberately out, and it is what forced the mirror in the first place, because coming back from orange to deep blue crosses grey and there is no way around that inside one strip. Ten colour bands across the pill became four, which is the other half of why the motion now reads as direction, and it travels rightward, with the page. 16 seconds a pass.
  Verified on the live apex at 375 and 1440 in FR, NL and EN: text version gone from every page, four distinct colours in nine stops that close on themselves, the strip exactly twice the pill's width to the sub-pixel (429.031 against 214.516) so the loop is seamless, pill still 30px tall with an even 2px stroke and its fill identical to the page, 50px from the pill's top to the H1's, left edge flush with the H1. No page overflow, no console errors.
- **2026-08-27 · Design session, the pill's stroke, fourth and last attempt (Claude Code, Opus 5, evening, same session).** Killing the palindrome last round was not enough: Fady still saw it "side by side duplicated", and he was right for a reason that turned out to be GEOMETRY, not colour. The strip was exactly two cycles wide and the pill shows half the strip, so a WHOLE CYCLE was on screen at once and its first and last colour sat at the two ends of the pill together. Any full cycle in view shows its endpoints twice, whatever colours you pick, which is why three rounds of recolouring never fixed it. The cycle is now THREE PILL-WIDTHS long and only a third of it is ever visible (measured on the live page: ring 6x the pill, visible fraction 0.333), so nothing can repeat across the pill and the ramp reads as one thing travelling through rather than a pattern sitting there. He also asked for a leader with the others following, and for gentler colour: it is a teal field with a narrow bright aqua band leading and a soft blue trailing, and the logo's dark navy #0F4A6A and mid blue #2E6FB1 are out for being too heavy. Four colours, all from the logo, all in its lighter register: #22B3A6, #25FEC0, #26C4B4, #317CB9. 20 seconds a cycle, rightward. The strip still holds exactly two cycles, so translating by 50% moves it by one cycle and the loop stays seamless.
  Spacing, his third pass at it: padding 4 to 3, margin 12 to 6, so the pill is 28px tall instead of 30 and the gap under it is 14px, smaller than the space inside it, which is what makes it read as part of the headline rather than floating above. From the pill's top to the H1's is 42px, against 62 when the pill first shipped.
  Verified on the live apex at 375 and 1440 in FR, NL and EN: ring exactly 6x the pill and a third of a cycle visible in every language, four distinct colours, 20s, pill 28px tall with an even 2px stroke and its fill identical to the page, 14px to the headline, left edge flush with the H1. No page overflow, no console errors. THE STANDING AGREEMENT: if this one is not right, v1 ships the plain text label with no effect.
- **2026-08-27 · Design session, v1 label settled and the share card rebuilt (Claude Code, Opus 5, evening, same session).** THE PILL IS OUT. Five rounds went into the hero label, a pill with a moving brand stroke and then a text version with the colour clipped inside the letters, and the standing agreement was that if the last attempt still did not read right, v1 ships the plain label with no effect. It does. `.eyebrow` is back to exactly what it was, with the shorter copy Fady picked in the widget ("Débouchage 24h/24" / "Ontstopping 24/7" / "Drain unblocking 24/7"); every trace of the pill and the text version is deleted from the template and the stylesheet.
  THE SHARE CARD, before Fady sends the link to Roro. The image was the van; it is now the LOGO ON WHITE, 1200x630, composed locally with ffmpeg. One detail was worth the trouble: overlaying `icon-512.png` straight onto white left a faint grey outline of its rounded square, which is very likely the "white square without rounded corners" Fady reported, so the mark was cropped out of that icon first and only the bare logo sits on the white. Built from a viewed image at every step (AGENTS section 7), and the old van banner is kept at `_scratch`-side as a backup.
  THE SHARE TITLE IS NOT THE PAGE TITLE, and that is the point. A `<title>` is written for a search result, where the brand can come last; a WhatsApp card is written for someone a person they know just sent a link to, and it has to say WHOSE link it is first. So og:title now leads on the name and carries the service and the zone (FR "Pro Débouchage · Débouchage 24h/24 autour de Bruxelles", NL "· Ontstopping 24/7 rond Brussel", EN "· Drain unblocking 24/7 around Brussels") while every `<title>` tag is untouched. The language pages were taking a SECOND code path (`head(lang, LANDING, c.meta)`) that fed them `meta.ogt`, the pain headline, as the share title: out of context that reads as a complaint, and it is no longer used for sharing. og:image:alt was still describing the van in all three languages and now names the logo. A PNG icon link now sits beside the SVG one, since a crawler that cannot read an SVG favicon has nothing else to fall back on, which is the other candidate for the square Fady saw.
  Verified by fetching the live pages with a WhatsApp user agent: all four pages (chooser, FR, NL, EN) return the new og:title, the logo image and the right alt, the image is byte-identical to the local build, and both the PNG icon and apple-touch-icon return 200. Label plain in all three languages at 375 and 1440, no page overflow, no console errors on a fresh tab.
  STILL OPTIONAL, his call: the share image is the mark alone, with the NAME carried by the title beside it. The wordmark can be drawn into the image itself in the site's own font if he wants it there too.

- **2026-08-27 (evening) Walkthrough-support session (Claude Code, read + filing).** Wrote the EN+AR call script for the Roro walkthrough from the built site. Roro's feedback filed: "From" prices until his list lands (meetup 2026-08-28), Afrim not Afrem (fixed in business-brief and NOW; site rename goes to the design session), van images must lose the phone number (Fady does it himself, NOW). Braine-le-Chateau CONFIRMED present on the site (three town lists + schema) and in the ads locations (research/25); Roro just missed it. Accountant update logged (VAT office working the case). Final design-session prompt written in chat; approval and audit plan in NOW and DECISIONS.
- **2026-08-27 into 2026-08-28 · Design session close: Roro's two copy changes and the full standards pass (Claude Code, Opus 5, working directly in design/site-source/, sole deployer).** EVERY LISTED PRICE IS A MINIMUM until Roro's own price list lands: the five rows carry "À partir de" / "Vanaf" / "From" as a quiet uppercase line above the amount, and the camera-alone 149 became "démarre à" / "vanaf" / "starts at" in the FAQ. The 60 euro failed-callout fee is untouched and exact, and so is the phone quote: the promise line still reads "le prix annoncé au téléphone est le prix sur la facture" in all three languages, because that is the one thing the whole page is built on and Roro's change does not touch it. The prefix sits OUTSIDE `.p-amt` on purpose so the yellow marker stays under the figure alone, and it rides the aria-label too ("WC bouché, À partir de 129 €, Appeler"). Every sentence that touches prices was swept: `priceIntro` now says the list carries starting prices and the exact one comes on the phone, `terms` says "prix de départ" / "vanafprijzen" / "starting prices", and the cost FAQ ends by saying the exact price is the one on the invoice. THE STRUCTURED DATA followed: the business node had no price claim at all before, only `priceRange`, so it could not contradict the page but it could not support it either. It now carries `makesOffer` with a `PriceSpecification` and `minPrice` per service, parsed from the same copy the rows render so the two cannot drift; there is no bare `Offer.price` anywhere, which would have asserted a fixed amount. JSON validated on all three live pages.
  THE TECHNICIAN IS AFRIM. It had been Afrem on every page since the first build: the who-section heading, his chat bubble, his job title, the avatar alt, the paragraph, plus the comments that ship inside styles.css and the build's own face-photo warning. Zero occurrences of the old spelling remain in the source or in any built page. `_archive` and git history untouched, as instructed. THE VAN IMAGES AND THEIR ALT TEXTS WERE NOT TOUCHED: the real van carries no phone number, Fady edits the image tomorrow morning and the alt rewrite rides that swap.
  THE CLOSE CHECK, design/05 section C, run against the live apex. PASSING: 20 tel links all `tel:+32480649649`; 23 unique data-cta; hreflang trio plus x-default, self-referencing; one H1 per page; 70 images with an alt and explicit width and height on every one; zero third-party requests before Accept and zero Google scripts, with only the denied consent defaults in the dataLayer; footer carries legal name, seat, e-mail, enterprise number and the VAT line; privacy AND terms linked from every page in every language; no em dash anywhere in the built site; no lorem, no TODO, no "Photo à venir"; no "toute la Belgique" and no "heel België"; skip link first in the body; 11 FAQ items; year 2026 with JS on; no horizontal overflow at 320, 375 or 1440 in FR, NL or EN; all six security headers present; every legal page, the sitemap and robots answer 200; `/nope-abc-123` returns a real 404; `*.pages.dev` still answers noindex. ONE DELIBERATE EXCEPTION to C5: the "send us your review" WhatsApp link carries no data-cta on purpose, because the conversion taxonomy is binary and a review click would otherwise be counted and paid for as a lead.
  TWO THINGS THE PASS ACTUALLY FIXED. The chooser and the 404 carried no e-mail and no legal links at all, which matters because the chooser is what a bare prodebouchage24.be resolves to and is the first page anyone sent the link sees: both now carry the company line, the e-mail and all six privacy and terms links, trilingual, since neither page has picked a language. And THE COMMENTS STOPPED SHIPPING: styles.css is inlined into every page and its long explanations were going out with it, 76KB of CSS against section C's 25KB budget with roughly two thirds of it prose. The build now strips comments from the SHIPPED copy only, reusing the same parse the integrity check already had to do, and asserts the strip did not grow the file or eat every rule. 76KB to 43KB, page 170KB to 136KB. Verified after the strip, because this is exactly how a stylesheet dies quietly: all four weights still measure distinct (400/600/800/900 at 228/236/255/271 px), both fonts load, the price and service markers still paint, the guarantee band is still yellow, the call bar is still fixed, 523 braces balanced, no console errors on a fresh tab.
  STILL NOT VERIFIED, and not claimed: section C14's Lighthouse 95, LCP under 2 seconds and the 250KB first-load budget cannot be measured from here, and the inlined CSS is 43KB against a 25KB budget even after the strip. C13's "the OG image renders in a real WhatsApp preview" was verified by fetching the tags with a WhatsApp user agent, not by pasting the link into WhatsApp. research/29 is closed except B10, which is Fady's two clicks (GA4 retention to 2 months, confirm Google Ireland terms) and is already on the orch list. THE SITE IS LIVE BUT NOT APPROVED; approval comes Friday night.
- **2026-08-28 · Friday orch, launch eve (Claude Code, Fable 5, /pro-orch, five Opus agents).** Morning block: cookie round CONFIRMED already live from the night build (audit agent, all research/29 points pass on the apex, only B10 left), ads pre-enable checks all CLEARED read-only (RSAs and sitelinks out of review, Tobacco appeal WON, conversions normal, first GA4 data), pd-review and pro-debouchage-v3 Pages projects DELETED, playbook/site-companion.md written, VIES still INVALID. Midday, on Fady's photos: real sunrise van shipped in the who section (columns measured equal on desktop) and the ticker, every printed-number van image retired to to-delete/2026-08-28-van-round/, alts and photoNote rewritten truthful, Fady's Figma share banner live as og:image (fresh URL og-banner-van-2.jpg after his ratio fix). Afrim's photo landed: 12 Magnific portrait candidates (3 styles, ~900 credits), Fady picked the teal studio one, integrated live as the bubble avatar with honest photoNote clause; the last build warning is gone. Decisions filed (149 floor stays, no outbound scam link, projects deleted, cookie round to orch). Session closed for continuity; next session /pro-orch-mid (approval night, full audit, visit planning).

- **2026-08-28 (late) · Four spotted-on-the-live-site fixes (Claude Code, Opus 5; styles.css, the avatar image set, build.js and template.js).** AFRIM'S AVATAR, TWO ROUNDS IN ONE. First the ring: `.bub-av` carried `padding:3px` behind a gradient on a `border-box` element, so the stroke was drawn from the INSIDE, the portrait box shrank by 6px and the circular mask then ate into his head. Fady's word on seeing it gone: the stroke was nice, put it back OUTSIDE. It is a `::before` at `inset:-3px` now, carrying the same ink-to-teal-to-yellow gradient and the drop shadow, with the image on `position:relative` above it; the photo keeps its full box (58px on a phone, 76px on desktop) and the ring grows outward to 64 and 82. Never make it padding again. Then the picture itself: Fady rounded the RAW file, it looked right, and he asked for that one. The shipped set was a tighter crop of that raw, cutting the top of the head and the shoulders, so all seven files were rebuilt with ffmpeg from `assets/raw/afrim-portrait.jpeg` as the full frame squared (2005x2005 out of 2005x2006), nothing else. THEY SHIPPED UNDER A NEW NAME, `afrim-avatar-2-*`: the first deploy reused the old filenames and the live apex kept serving the old picture from the Cloudflare edge (`cf-cache-status: HIT`, max-age 86400) even though the deployment held the new bytes, which is exactly the trap the og-banner rename dodged earlier the same day. build.js's manifest and template.js follow the new base; the superseded crop sits in `to-delete/2026-08-28-avatar-recrop/` with a note (recovered from the CDN, since the locals were overwritten in place before the rename). THE MOBILE RHYTHM IN THE WHO SECTION: the van sat 58px under the chat bubble while the bubble sits 40px under the sub text, because the one-column grid gap (28px) and the figure's own `margin-top:30px` were stacking. `.van` margin dropped to 0 and the mobile `.who-grid` gap raised to 40px, so both gaps now measure 40 live; the desktop query overrides the gap and its now-redundant margin reset went with it. THE THREE "notre façon de travailler" CARDS: `.inc-card h3` was the smallest card heading on the page at 18.7px on a phone and now rides the same ladder as the scam band's heading, clamp(1.25rem, 1.1rem + .75vw, 1.5rem), so 20.4px on a phone and 24px on desktop. A step up, still well below the section headings. VERIFIED on the live apex after three deploys: all seven new avatar files answer 200 and are byte-identical to the local build, the FR, NL and EN pages all reference the new name and carry all four CSS changes, photo 58/76 with the ring at 64/82, gaps 40 and 40, heading 20.4 and 24, desktop columns still stretch equal (412px each), no horizontal overflow at 375 or 1280, no console errors. The old avatar URLs are referenced by nothing and expire from the edge on their own.

- **2026-08-28 (afternoon) · FULL FOUNDATION AUDIT and fix round (Claude Code, Fable 5 orchestrating, four Opus agents; session crashed mid-turn with the app, recovery session completed it).** Audit verdicts: site GREEN (live byte-identical to the build on 10 pages, 126 assets 200, consent gating proven on the wire, load burst 300/300 at ~0.25s p50), ads NO BLOCKERS for Saturday (three watch items resolved; five real to-dos filed in NOW: manager link missing, info@ without 2SV, no backup card, one verification sub-task, Sat 11:00-13:00 Cloudflare maintenance), best-practices research filed as research/30, file health found the stale 115 MB worktree (Fady's click, NOW) and 12 superseded files (moved to to-delete/), sync fixes filed across accounts, launch-plan, separation, landing-page, ads-program, STATE, INDEX and AGENTS.
  The www fix: `_redirects` was tried and Cloudflare Pages ignored it, so www-to-apex is now a zone Redirect Rule "www to apex" (301, path and query preserved), verified on the wire; the dead `_redirects` was removed from build.js and the site redeployed clean. The crash wiped wrangler's OAuth login; re-granted on Fady's yes (his consent, one Authorize click in his Chrome). Nothing was lost to the crash: the uncommitted diff was exactly the ads filing, verified line by line.

- **2026-08-28 (late afternoon) · Close-the-open-items orchestration (Claude Code, Fable 5 orchestrating, three Opus browser agents plus Fady's clicks).** The never-bend rules are enforced hooks now (.claude/settings.json + three Node scripts: no git, no edits to site-v1/ or taxi-business/, em-dash check; tested and live-proven). The 115 MB stale worktree FOLDER deleted directly (no git needed); only `git worktree prune` + branch delete remain, Fady's terminal. Ads manager link LINKED Aug 28 (agent sent the invite from 724-595-2027, Fady accepted through the identity check, verified in the client's Managers list). GA4: retention 2 months, DPA confirmed accepted 27 Aug (B10 CLOSED), hi@fady.be account-level Administrator and backup4fady@gmail.com Administrator (Fady's clicks). Search Console: hi@fady.be Owner. Gmail: signature "Pro Debouchage" live as default; smart features ON by Fady's quick click, reversible.
  Also: accountant VAT message drafted (5 questions), depot-visit checklist handed (GBP video spec, 2SV plan with Fady's number after the video, card swap, KBO name ask), enable plan set (Claude clicks enable at Saturday's morning orch on Fady's word). V1 FULLY APPROVED: approval plus audit findings all fixed. NOW rewritten (60 to 31 lines), accounts.md updated in four rows.

- **2026-08-29 (morning) LAUNCH DAY orch (Claude Code, Fable 5, /pro-orch; 3 Opus voice researchers, 1 Opus editor, Fady's Chrome for Ads).** Visit outcomes filed (GBP video submitted, verdict clock ~09-02; card swap done; 2SV moved to a remote call; VAT questions and the Steph Tally form SCRAPPED, decisions filed). Pre-launch VOICE ROUND on Paolo's "sounds translated" note: research/31-33 (FR-BE bricozone+reviews, NL-BE bouwinfo+reviews, EN expat register), Fady approved the from-to table, ~65 phrase edits shipped in the three copy files plus wij-to-we sweep, ledigen-to-ruimen, toezichtsput-to-controleput, and the review name Paolo-to-François; built, deployed (8fc427f8), verified live string by string with layout checks at 375/1440, promise and 60 EUR lines untouched. Ads affiliation sub-task attempted: email path rejected by Google's third-party match, parked for the 2SV call (SMS to 0480 649 649). **CAMPAIGN 24185896982 ENABLED ~08:00, Eligible, Presence-only confirmed, 20 EUR/day; launch date written into ads-program.md 5.** VIES polled: still INVALID.

- **2026-08-29 (morning, after launch) · Afrim's avatar swapped for his real photo (Claude Code, Opus 5; one update, nothing else).** Fady dropped `assets/raw/afrim-portrait-real.jpg` (1831x1832, his own unretouched photo, hedge background) into raw. The seven derivatives were rebuilt with ffmpeg as the full frame squared (1831x1831) and resized, nothing else: 96/192/384 in avif and webp plus the 384 jpg fallback. THEY SHIP AS `afrim-avatar-3-*`, a new base, because the `-2` URLs were already warm on the Cloudflare edge (max-age 86400) and reusing them would have kept serving the teal studio portrait. `build.js`'s manifest and `template.js` follow the new base; the superseded `afrim-avatar-2-*` set sits in `to-delete/2026-08-29-avatar-real/` with a note. Built, deployed (f743fb0f), VERIFIED on the live apex: FR, NL and EN all reference `afrim-avatar-3` and nothing else, all seven files answer 200 byte-identical to the build, and the bubble image resolves to `afrim-avatar-3-192.avif`, renders 76x76 with the 50% mask, hit-tests as the IMG and paints skin tone dead centre of the circle. ONE THING LEFT FOR FADY: the footer photoNote in all three languages still says Afrim's portrait was "prepared from a photo of himself", which was true of the Magnific portrait and is now over-disclosure; not changed, because the ask was one update.

- **2026-08-29 (same morning, follow-on) · photoNote clause dropped (Claude Code, Opus 5).** On Fady's word, the "and Afrim's portrait was prepared from a photo of himself" clause came out of `photoNote` in all three copy files. It was true of the Magnific portrait and became over-disclosure the moment his own unretouched photo shipped as the avatar. The note now discloses only the recreated "before" photo, which is the one thing left to disclose. Built, deployed (2821f80c), verified live: the shortened sentence reads correctly in FR, NL and EN on the apex and zero occurrences of the dropped clause remain anywhere in the built site or on the live pages.

- **2026-08-30 · INVOICE session (Claude Code, Opus 5 orchestrating, six Opus agents plus one Sonnet extraction; no site touched).** LEGAL RECHECK of all three existing .docx against research/26: the 6 percent declaration is character-identical to the official text in FR (812 chars) and NL (809), one single run, no break inside it, in every file, which was the main worry and it was clean. Real gaps found and fixed: the reverse-charge sentence was ABSENT while CGV article 10 states in writing that it appears on the invoice; no acompte reference; the late-payment line read as if the 14 days could run from the invoice; the page-2 customer block was mislabelled FACTURÉ À instead of CLIENT. V1 also printed the company's own e-mail as the consumer mediation service's address and carried a scraped "Retour à la page d'accueil" nav line; both fixed. NL still carries reinigen where FR says curage (rate risk, research/26 section 5) and is one service row short: both filed for the NL build.
  THE FILL PROBLEM DIAGNOSED. Fady's complaint that the template was "weird to fill" was correct and it was not Word: the files contained ZERO form mechanisms (0 content controls, 0 form fields, no protection). The date spot was literally two spaces, slash, three spaces, slash, space, 2026, so typing pushed the year sideways and backspace ate the mask; the tick boxes were the ☐ character typed as text. Research established the decisive constraint: **Word for the web opens any protected document read-only**, so the classic locked Word form would have locked Fady out of reviewing. Build answer: no documentProtection anywhere, 71 fill fields as plain-text content controls with sdtLocked plus real glossary docParts, 14 real w14 checkboxes, 19 contentLocked wrappers around the law text, totals hand-typed on purpose (Word table formulas do not work in the browser at all and print a stale number in desktop Word without F9), dates as plain-text controls not date pickers, six pre-printed service rows kept.
  SHIPPED: `PRO-DEBOUCHAGE-facture-A-FR.docx` and `PRO-DEBOUCHAGE-facture-B-FR.docx` plus a KEPT, re-runnable `build-invoice.js` (+ `cgv-invoice-fr.js`, `cgv-invoice-nl.js` and the logo), which never existed before; the 2026-08-27 pack was a throwaway script. The builder opened both files through real Word by COM, exported PDF and read the rendered pages, which caught three corruptions it had shipped in its first attempt (duplicate default paragraph style, three drawings sharing wp:docPr id 1, cells ending in a nested table) plus page 1 overflowing to page 2 and an internal alias printing as a date label. Verified independently by the orchestrator afterwards: no documentProtection, 90 content controls with 90 unique ids, glossary placeholders all resolving, 6 percent sentence 812 chars, reverse-charge 393 chars, no legacy .com, mediation address correct.
  THE REGISTER FINDING, the serious one. VIES polled 2026-08-30 10:27 UTC returns isValid false with no name and no address, and the KBO shows exactly two qualities on the company (ONSS employer since 2026-03-12, entreprise soumise à inscription since 2025-10-01) with NO "Assujetti TVA" entry. Meanwhile the real invoice Roro sent as the example charges TVA 6 percent (264,15 HTVA + 15,85 TVA = 280,00 TTC) under N° TVA BE1027.454.187. Question drafted in French for the accountant, filed in NOW as urgent. The invoice identity line is a single constant so it flips in one edit either way. That same example invoice also revealed it is titled FACTURE while its number box is labelled N° DE DEVIS, and still carries the legacy INFO@PRO-DEBOUCHAGE.COM address, and says "payment by transfer" and "settled, paid in cash" on the same page.
  ALSO FILED: `playbook/invoicing-flow.md` (the per-job flow, the 12-line WhatsApp template Roro sends Steph, the Peppol STOP test for Belgian VAT-registered business customers, the open questions). Official Dutch reverse-charge wording located in the enacting text of the KB of 26 October 2022 (NUMAC 2022042522) and cross-checked against the FR of the same instrument, ready for the NL build. BIC confirmed BBRUBEBB (ING) from the NBB list, deliberately not printed. The devis research was launched then killed once Roro confirmed they use only the one bill document.

- **2026-08-30 (same session, close) · Invoice pack finalised and the e-mail prepared.** Fady chose LAYOUT B (FACTURE beside the small logo), which was also the recommendation. B promoted to `PRO-DEBOUCHAGE-facture-FR.docx`; layout A and the 2026-08-27 pack moved to `to-delete/2026-08-30-invoice-layout-choice/` and `to-delete/2026-08-30-invoice-2026-08-27-pack/`. THE DUTCH TWIN IS BUILT, `PRO-DEBOUCHAGE-factuur-NL.docx`, carrying the official NL reverse-charge sentence from the KB of 26 October 2022 and the missing 6th service row; verified independently (809-char NL 6 percent declaration, BV, RPR Brussel, the six NL recap labels, 90 controls with unique ids, no protection). The NL "reiniging" wording was left alone on purpose: it matches the live site, and the agent was told to read the site rather than invent a Dutch term. THE LOGO WAS 90 PERCENT OF EVERY FILE (164,703 bytes for a 530x330 image). Recompressed losslessly to 90,082 as `logo-pro-debouchage-2.png` (new filename per the AGENTS rule), proven pixel-identical on the visible pixels, on alpha and composited on white, with 10 of 11 package parts byte-identical; the three documents dropped to 110,614 / 110,547 / 96,890. Also shipped: `PRO-DEBOUCHAGE-guide-facture-FR.docx`, one page, French, opening with the Protected View click and the save-a-copy habit. GMAIL DRAFT created in hi@fady.be to info@prodebouchage24.be, body verbatim, NOT sent, and WITHOUT attachments: the three files base64 to about 424,000 characters, which the tool call cannot carry, so Fady attaches them by hand. Roro call script handed over in chat, including the VAT register point.

- **2026-08-30 (Sunday) Orch session (Claude Code, Fable 5, /pro-orch; 2 Opus read-only agents).** Sweep found HANDOFF.md two closes stale; rewritten. Ads day-2 glance: 51 impressions, 4 clicks, CTR 7.84 percent, CPC 1.86, spend 7.42 EUR of 40, billing clean, everything Eligible, NOT budget-limited; ONE flag, "Eligible (Limited)" because the goal has no primary conversion action under Maximize conversions (Smart Bidding serving conservatively); Fady asked fix-now vs week-2 via widget. VIES 15:44 UTC still INVALID, KBO no VAT quality. Fady's call: the .com becomes an ACQUISITION, Roro-first (van QR + prints point to it), redirect to prodebouchage24.be; decision and print-refresh lines filed, WhatsApp draft handed over.
- **2026-08-30 (Sunday, follow-on) Conversion-goal fix (Opus browser agent, one authorized change).** "Calls from ads" set PRIMARY in the Phone call leads goal (was Secondary, goal Misconfigured with 0 primary). Campaign 24185896982 now reads plain Eligible; the "missing primary conversion action" diagnostic is gone. Nothing else touched. Left open: whatsapp_click still Secondary in the Submit lead forms goal (that goal still Misconfigured); Fady asked whether it becomes primary too. Verify the Eligible status again at Monday's weekly check.
- **2026-08-30 (Sunday, second follow-on) whatsapp_click set PRIMARY (Opus browser agent, one change).** Fady delegated, Claude decided yes (DECISIONS 2026-08-30). Action optimization now "Submit lead forms, Primary action"; all three goals read 1 primary / Active, none Misconfigured; campaign 24185896982 still plain Eligible. One stale banner ("2 goals cannot be used") remained on the Conversions summary, contradicted by the live per-goal rows; re-check at Monday's weekly check.
- **2026-08-30 (Sunday, close) Day close on Fady's comments (Claude Code, Fable 5).** CLOSED: the invoice e-mail is SENT (print test tonight at Roro's, feedback follows); Steph-email ask dropped (all contact via Roro); Claude Design publish item and the Afrim bigger-image idea DISCARDED; VAT downgraded from urgent (accountant: active, display lag, weekly poll only). RESHAPED: the .com WhatsApp became guidance on what Roro asks printpress (transfer, not login), draft v2 handed; NL wording gets a native Fiverr check once clients flow. HANDED: the 2SV close-out session prompt and the two git Run buttons. NOW rewritten (35 lines), HANDOFF rewritten for Monday /pro-orch. Ads confirmed on plan: Eligible, both lead signals primary, Monday weekly check next.

- **2026-08-30 (Sunday, evening) 2SV CLOSE-OUT session (Claude Code, Opus 5, /pro-orch-mid; 1 Opus browser agent, everything else Fady's own clicks).** 2SV on info@prodebouchage24.be is ON and closed: passkey on Fady's Windows, authenticator on his iPhone, Google prompt on 2 devices, 10 backup codes in his Bitwarden. Two planned items were deliberately DROPPED, not missed, once Fady stated the control model out loud: the agency manages end-to-end and Roro is the owner on record, so second factors stay agency-side and a passkey on Roro's phone plus codes on his side are not wanted. The SMS number was dropped on advice too: with a passkey, an authenticator and backup codes already in place, adding SMS contributes only the weakest factor and a SIM-swap path (Fady's own worry, that reusing his number across Google accounts looks fraudulent, is not a real thing).
  ADS AFFILIATION, the long one. The agent found the sub-task at Admin > Policy > Account, not at /aw/policymanager/verification which 404s, and reported the real blocker: Google matches against its own Dun & Bradstreet record for PRO DEBOUCHAGE SRL, so 0480 649 649 was rejected at the pre-check with NO SMS ever sent, and so were Fady's number, hi@fady.be and info@prodebouchage24.be. Google then escalated to "Verify with your ID", a government ID of a known director. Fady held Roro's card from an earlier task, redacted the photo and the rijksregisternummer, and submitted. No acknowledgement appeared, so it is UNCONFIRMED; re-check at the weekly check. The classifier blocked the agent from submitting the form, as it does for every access-grant click, so that step was Fady's by necessity. Nothing else in the Ads account was touched.
  THE NEAR MISS. Roro's ID card was sitting in `assets/raw/` as Front.jpg and Back.jpg, dropped at 20:34 while the task was being worked. `assets/` is on the backup whitelist, so both were one backup run from entering the version history permanently, and the back of a Belgian eID carries the rijksregisternummer. Caught before any commit; Fady deleted them and keeps his working copies outside the folder. Verified gone, and no other non-markdown file entered the folder tonight. Filed as an explicit rule in DECISIONS.
  NOT DONE, open rather than dropped: the KBO commercial-name question was never asked (Roro went out with friends; Fady expects a yes), and the Gmail MOBILE signature on Roro's phone is UNVERIFIED, because the Gmail app ignores the web signature and needs its own Mobile signature setting which nobody has confirmed was tapped. Depot photos re-filed against the GBP verdict.
  RULE APPLIED on Fady's yes at the close: AGENTS.md section 7 gains "IDENTITY DOCUMENTS AND PERSONAL PAPERS ARE HANDLED OUTSIDE THIS FOLDER, redacted copies included", with tonight's near miss named in it as the reason. It was luck and timing that caught the ID card, not a rule, and now there is a rule.
  CORRECTION, same evening: the affiliation submission was ACCEPTED, not pending. Fady re-opened Admin > Policy > Account and Completed tasks reads "You've confirmed your personal affiliation with the organization, Answered on Aug 30, 2026". Advertiser verification is fully closed, all four tasks Completed, no clock to carry. Filed wrong for an hour because the submit gives no success screen and the session did not have the page re-read after the submit; the result only ever appears in the Completed tasks list. STATE, NOW, HANDOFF, accounts.md and DECISIONS all corrected.
  FOLDER HEALTH AUDIT and FACT-SYNC (2 Opus agents, one read-only auditor and one editor, claims spot-checked by the orchestrator afterwards). The audit found the live files quietly out of step in ~17 places while STATE, NOW and HANDOFF looked clean. Fixed in 26 edits: `accounts.md` still said the campaign was PAUSED and Fady's card was the payer (both account-critical and both wrong since 2026-08-28/29), the Business Profile row still described the depot video as a future event, and the Claude Design row still carried a discarded task; three playbook files still named the featured review Paolo when the live site has said Francois since 2026-08-29, `site-companion.md` among them, which is the script read aloud to Roro; "Afrem" survived in eight live places after Roro corrected it to Afrim on 2026-08-27; STATE contradicted itself two bullets apart on whether the camera is free; `business-brief.md` carried a superseded VAT story and two delivered tasks; the invoice README named two deleted files, stated a numbering rule the decisions had replaced, and claimed the folder held two files when it holds seven. The work-order question was answered by unzipping both live .docx rather than guessing: page 2 IS still there in FR and NL, only the filenames dropped the words. `design/06-assets-manifest.md`, a v2 leftover pointing at deleted files, moved to `_archive/2026-08-26-lock-in/`, so `design/` holds only `site-source/` as the folder map says. Two closed status lines left NOW.md. LOG and DECISIONS untouched, verified by mtime.
  TWO REAL RISKS CLOSED, both Fady's call at the close. (a) `.claude/hooks/` was NOT on the backup whitelist: the three scripts that ENFORCE the never-bend rules (`deny-git.js`, `no-emdash.js`, `protect-paths.js`) had no cloud copy while the rules they enforce did, so a dead machine would have kept the prose and lost the enforcement. Added, along with `.claude/settings.json`, and `.claude/hooks/` is now named in the AGENTS section 3 folder map, which is why it was missed. (b) `assets/raw/facture-exemple-17803.pdf`, a REAL customer invoice naming a client at 1080 Molenbeek, was inside the backup-whitelisted `assets/` tree. Moved to `to-delete/2026-08-30-customer-invoice-pdf/` with a README recording why and confirming every fact it taught us is already in the files. Half a fix by design and Fady knows it: the copying stops, the data stays on his disk until he empties the folder. Clean bill otherwise: site source intact (13 files, none empty), `research/INDEX.md` exactly in step at 33 of 33, no identity document anywhere, no stray or zero-byte files.
  FIVE RULE IMPROVEMENTS APPLIED at the close, all on Fady's explicit yes (2 Opus agents on disjoint file sets, claims spot-checked afterwards). (1) **Byte budgets replace line caps**: STATE 14 KB, NOW 8 KB, HANDOFF 4 KB, checked with `wc -c`, because a line cap measures nothing when one line is 350 characters (STATE sat at 27 percent of its 120-line cap while costing 11.9 KB to read). A single LOG entry now targets 2,500 bytes; LOG.md is 148 KB with twelve entries over 2,000 characters and one at 6,162, which is why /pro-orch can only afford the last entry. (2) **VERIFY THE RESULT, NEVER THE SUBMISSION** (AGENTS 7): a submit is not an outcome, the page is re-read before anything is filed, with tonight's affiliation error named in the rule. (3) **A FACT-SYNC AUDIT RUNS AT LEAST ONCE A WEEK** (AGENTS 7): the live state files are read every session so they stay honest, the playbook files are read on demand so they rot silently; a read-only agent cross-checks every playbook file against STATE, NOW, DECISIONS and the site source, weekly at the Monday /pro-orch and always after a session that changed an account, a price, a name or a live surface. It reports, it never fixes alone. (4) **Section 4 gains two owner rows**: `invoicing-flow.md` (invoicing and quotes) which existed and was pointed at while being in neither the folder map nor the fact table, and `separation.md` for the custody model. (5) **The one-editor rule corrected**: Fady REPLIES from his phone, so phone and cloud sessions drive real edits and "only one window is open" proves nothing; every session now re-reads the three live files immediately before its first write of a stretch of work, and merges rather than overwrites when they moved. All three skills updated to match.
  CONSOLIDATION, same close. `playbook/separation.md` becomes the ONE owner of the control model (new section 0, plus 0b for how the work runs) and the four full restatements in accounts.md, STATE.md and NOW.md became one-line pointers; the section spells out that custody being agency-side does NOT weaken G2, because ownership is the part that is unchanged, and it records the handover as one pass. Roro's growth signal filed as a client fact: **he said that if this goes well he MAY BUY A SECOND VAN**, said not promised, no date and no number (rule 1). New `ads-program.md` section 7 defines what "working" means so no future session optimises the wrong thing: jobs per month is the success condition, clicks and CTR are diagnostics, the ceiling is Roro's capacity, and NO target number exists yet because G6 forbids one before week-8 evidence. The second pass found FIVE more contradictions the first pass had not touched (both conversion actions still listed SECONDARY after their 2026-08-30 promotion, the dropped daily Tally form still planned for week 2, the affiliation cell contradicting itself, "agency price not decided" against the 2026-08-23 decision, the card swap listed as future). That is the argument for the weekly audit in one line: two passes in one evening and the second still found five. Also validated in passing: `deny-git.js` blocked an agent that reflexively appended a version-control command, so the enforcement layer works, which is the layer that had no cloud copy until tonight.

## 2026-08-31 (Monday, first weekly check; closed 2026-09-01 morning)

- First weekly ads check (read-only, agent on Fady's Chrome): Sat-Mon 73 impressions, 7 clicks, CTR 9.6%, CPC 2.36, spend 16.50 EUR, campaign plain Eligible, "2 goals" banner gone, billing clean on Mastercard 2616 (no backup card, filed). One call_click conversion Sun filed UNVERIFIED (possibly Fady's test). Weekly Roro report finalized REPORT-ONLY on Fady's word; all asks queued as separate follow-ups.
- Monday fact-sync audit: 19 findings, 16 fixed by an edit agent and verified on disk (Roro script's Afrim photo claim, "free" camera wording, secondary-conversion leftovers, François into business-brief, separation.md payment overstatement, byte-budget headers, invoicing items into NOW). Consolidation proposals parked for eof.
- VIES poll: still INVALID 08:07 UTC, KBO Active with no VAT quality shown; unchanged, weekly poll continues.
- CLOSED: price list (Roro happy, From stays), depot photos (optional, v2), the two git Run buttons (worktree/branch verified gone on disk), to-delete emptied, .be whois (both privacy-shielded at OVH since 2016/2018, parked, unused, holder needs a disclosure request; no action).
- DECISIONS gained four 2026-08-31 entries (price list, photos, unverified conversion rule, report-style). Chrome connector pick "Fady's Chrome" settled for agent browsing.
- EOF (2026-09-01 morning): four consolidation improvements APPLIED on Fady's delegated yes (AGENTS 4 price-owner row; VAT status single-owner in STATE with pointers in business-brief and invoicing-flow; NOW gates shortened to one-liners pointing at launch-plan 4; launch-plan stale section-9 pointer fixed). Note kept: the "print enterprise number, not TVA BE" guidance now lives only in STATE line 8.

## 2026-09-01 (Tuesday)

- Day ran /pro-orch, sweep clean. Agents: GBP verdict STILL PENDING ("Processing, up to five days", read from the correct Chrome profile after the first check found info@ absent from Fady's Chrome); daily ads glance CALM (150 impressions, 14 clicks, CTR 9.3, 43.08 EUR since launch, Google restated the earlier 16.50, second unverified call_click Mon, billing clean, both ads Eligible); five Roro ask drafts produced.
- Browser problem SOLVED for good: Chrome profile map saved to Claude memory (Default = personal, Profile 3 = Taxi Henok, Profile 4 = Pro Debouchage with info@), profiles openable by command, so sessions stop asking Fady which browser. Lesson learned and saved: only ONE browsing agent at a time, two parallel agents collided on the same browser.
- Roro news filed in STATE and DECISIONS: FIRST job since launch (WC unblock, 129 EUR accepted, ~280 follow-up quoted and accepted in principle, ~60 km out, no bill wanted, call source unconfirmed so not claimed for ads); backup card DECLINED (one card, Roro's choice); branding polish PARKED until after the .com (branded e-mail signature, WhatsApp, consistency); asks go by PHONE; business card promised, now Claude's design task; Ads security tasks (allowed domains) done by Fady.
- Email-preference mystery closed: DECISIONS 2026-08-31 already defined it, ask Roro whether he wants the weekly updates by e-mail. Roro missed the call, so one combined WhatsApp (.com + KBO + email question + client-source question) was written for Fady to send; the task line sits in NOW.
- Fady's working note saved to memory: Claude decides operational details itself, only real decisions reach him. Two account-level badges (Ads security nudge now done, info@ "Important account alert") noted; the info@ alert is filed in NOW for a read-only look.
- Close addendum (11:20): on Fady's word the improvement rule changed (internal improvements auto-applied, only Fady-facing ones asked, AGENTS 10 + pro-eof 7), the browser rule entered AGENTS 7, summaries go bullet-point, and the wrong "evening" wording in HANDOFF and NOW was fixed to late morning.

## 2026-09-01 (Tuesday, second session)

- Full folder health audit ran: three read-only Fable agents (fact-sync, site and build, hygiene). Site and build FRESH and clean, source rebuilds byte-identical, prices in sync between the copy files and the live page. Hygiene clean: no ID documents anywhere, no secrets in any file, the three hooks proven live.
- The fact-sync audit found 16 stale facts, all fixed this session: accounts.md (Infomaniak row to the post-ID-upload state, lookalike .be row closed per the 2026-08-31 whois check, Ads row now says ENABLED and LIVE since 2026-08-29, backup card declined for good, billing on Roro's Mastercard 2616, Ads security tasks done by Fady 2026-09-01), ads-program.md (no negatives at launch, the preventive list waits for evidence at 30 to 50 clicks on Fady's yes; Roro reports carry only confirmed-real conversions, a few highlights never a list), business-brief.md (4 jobs incl. the first since launch, camera price attribution fixed, .com is an acquisition with redirect not a takedown, invoice print test still awaited), landing-page.md (S7 is the three-photo story, invoice retention corrected to 10 years), site-companion.md (price list task closed, the script no longer asks Roro for a list), and the invoice README (dead to-delete references updated, the folder was emptied by Fady).
- One real conflict found and filed for Fady in NOW: the live privacy page says invoices are kept 7 years, the law says 10 (art. 60 CTVA, research/26 section 7); legal.js fix plus rebuild and redeploy waits on his yes.
- LOG bloat noted as history: the file is large and some old entries run long; the per-entry cap (about 2,500 bytes) stands, nothing rewritten.
- Retention fix closed same session: Fady approved, legal.js corrected FR/NL/EN (sept ans to dix ans, zeven jaar to tien jaar, seven years to ten years), clean rebuild, deployed to prodebouchage24, all three live privacy pages verified over HTTPS saying ten years.
