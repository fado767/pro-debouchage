# HANDOFF.md
*Written by the last session for the next one. Overwritten at every close. Max 40 lines.*

**Written 2026-08-30 at close, after the dedicated 2SV session and a full folder health audit. Next
session: Monday 2026-08-31, a NEW day, `/pro-orch`, first job the FIRST WEEKLY ADS CHECK. This line
owns state and supersedes the earlier 2026-08-30 handoff.**

## What this session did
**2SV on info@prodebouchage24.be is DONE**: passkey (Fady's Windows), authenticator (his iPhone),
Google prompt on 2 devices, 10 backup codes in his Bitwarden, **no SMS number on purpose**. Fady
stated the control model and it is now a decision: **the agency manages end-to-end, Roro is the
owner ON RECORD**, so second factors live agency-side. That killed two planned items for good
(passkey on Roro's phone, codes on his side). Do not reopen.

**Google Ads advertiser verification is FULLY CLOSED.** Every automatic affiliation path was
rejected against Google's own Dun & Bradstreet record (0480 649 649 refused at the pre-check with
no SMS ever sent, same for Fady's number and both e-mail addresses). Roro's redacted ID was
submitted and **accepted the same day**. Note for next time: **that flow shows no success screen**,
the Completed tasks list at Admin > Policy > Account is the only place the result appears.

**Health audit, 26 fact-sync fixes applied.** STATE, NOW and HANDOFF looked clean while the playbook
quietly was not: accounts.md still said the campaign was paused and Fady's card was the payer, three
files still named the review Paolo when the site says François, "Afrem" survived in eight places.
Also closed: `.claude/hooks/` had no cloud copy (the rules were backed up, their enforcement was
not), and a real customer's invoice was sitting in the backup path.

**Five rule improvements applied on Fady's yes** (details in LOG): byte budgets replace the dead
line caps (STATE 14 KB, NOW 8 KB, HANDOFF 4 KB, `wc -c`); "verify the result, never the submission";
**a read-only FACT-SYNC audit now runs weekly at the Monday /pro-orch** and after any session that
touches an account, price, name or live surface; `separation.md` is the ONE owner of the control
model and the duplicates are now pointers; the one-editor rule rewritten because Fady replies from
his phone, so every session re-reads the three live files before its first write. Roro's growth
signal is filed: he MAY buy a second van, said not promised, and `ads-program.md` section 7 now
defines success as jobs per month, not clicks, with no target number until week-8 evidence.

## Monday session, in order
1. Weekly 20-minute check + the ONE-PAGE RORO REPORT (ads-program.md 6): pacing, search terms,
   negatives to PROPOSE (not apply), conversions sanity, 7-day snapshot.
2. Re-verify 24185896982 still plain Eligible and the stale "2 goals" banner cleared.
3. Poll VIES/KBO (weekly; accountant says active, display lag).
4. Ask Fady: did Steph's print test happen, any fix needed in build-invoice.js?
5. GBP verdict ~2026-09-02: then hi@ as Manager, location asset, citations sweep, REVIEWS push.

## Waiting on whom
Fady: the KBO commercial-name question (never asked, Roro was out, expects a yes); the Gmail
**Mobile** signature on Roro's phone (the app ignores the web signature, its own setting is at Gmail
> Settings > info@ > Mobile signature, one test mail settles it); .com WhatsApp v2; the two Run
buttons; empty `to-delete/` when convenient (10 folders, 8.5 MB). Roro: printpress ask, price list,
print feedback, depot photos in daylight after the verdict. Clocks: GBP ~09-02, .com 10-07, week-2.
