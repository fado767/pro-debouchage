# HANDOFF.md
*Written by the last session for the next one. Overwritten at every close. Max 40 lines.*

**Written 2026-08-27 evening after the ads build session (/pro-orch-mid). Same-day resume:
`/pro-orch-mid`. RULE REMINDER: execution goes to Opus agents when the classifier allows it; this
session's browser work ran in-session because the classifier refused the agent handoff (Fady's go).**

## What this session did (LOG has the full entry)
The campaign is PUBLISHED AND PAUSED (live id 24185896982). Budget was re-entered (20/day custom,
the draft had lost it), Ad group 1 renamed FR emergency, NL emergency built to research/25 (Google's
auto-generated FRENCH content fully replaced with the sheet's NL copy, final URL fixed to /nl/),
all section-6 assets in (FR+NL callouts, Services snippets, sitelinks, ad-group level; call asset
account-level Eligible), negative list attached, step-13 read-back PASSED. Users list: hi@fady.be
Admin invite SENT AND ACCEPTED. Advertiser verification: disclosure name "Pro Débouchage" is
CORRECT as-is (public trade name, no edit made); the SRL legal-name match happens in the pending
Dun & Bradstreet task.

## Watch items
- FR structured snippet shows a nonsense "Disapproved: Tobacco" flag; ads and sitelinks Under
  review. Appeal / recheck once the account unpauses. Nothing serves anyway (account paused for
  advertiser verification, campaign paused on top).
- Chrome kept resizing its window and dropping the extension mid-session; ref-based clicks
  (find → click by ref) survived it, raw coordinates did not.

## Next, in order
1. [Fady] Dun & Bradstreet verification task (Admin > Policy > Account > "Start task") with the
   KBO pack (BE 1027.454.187, Roro's ID). 48-hour document deadline once started; do it at a calm
   moment TODAY or tomorrow. This is what lifts "Account paused".
2. [Claude] TAG ROUND (G5): flag-on build + deploy + wire proof per research/25 step 10,
   coordinated with the design session (one deployer at a time).
3. Depot visit is TOMORROW Friday 2026-08-28 (pack on Fady's phone; GBP video + imagery).
4. Enable on Fady's word only, after the Roro walkthrough (G6 stays: no ROI promise).

## The DESIGN SESSION still runs in parallel (separate chip session, Fady-driven)
It edits design/site-source/ ONLY and appends its own LOG entry at close. This orchestrator owns
the state files. One deployer at a time.

## Still waiting elsewhere
Roro: Afrem's face photo (parked). VIES still INVALID. Fady calm-moment list: save-to-cloud,
invoice .docx eyeball, accountant VAT questions, GA4/SC leftover grants, .com takedown parked.
