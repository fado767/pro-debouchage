@AGENTS.md

Claude Code specific rules go below this line. None today, 2026-08-22: everything in AGENTS.md applies unchanged.

Start of every session: read `STATE.md`, `NOW.md` and `HANDOFF.md` (all three are small by rule), then the day's skill (`/pro-orch`, `/pro-orch-mid` or `/pro-eof`). Read anything else only when the work touches it.
