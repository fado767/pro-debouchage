@AGENTS.md

Claude Code specific rules go below this line. None today, 2026-08-22: everything in AGENTS.md applies unchanged.

Start of every session: read `STATE.md`, `NOW.md` and `HANDOFF.md` (all three are small by rule), then the day's skill (`/orch`, `/orch-mid` or `/eof`; the `pro-` prefixed names were retired on 2026-09-04 so every venture folder answers to the same three words). Read anything else only when the work touches it.
