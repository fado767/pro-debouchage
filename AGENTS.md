# AGENTS.md

*The working rules for this folder. Any tool with access to the folder reads this first. Created 2026-08-22, the kickoff session, modelled on the taxi business rules (`../taxi-business/AGENTS.md`) and deliberately smaller. Procedure lives in the three skills, facts live in the owner files below, this file only carries rules.*

## 1. The project, in three sentences

Pro Débouchage is a 24/7 drain-unblocking, sewer cleaning, camera inspection, septic emptying and flooded-cellar pumping service based in Vilvoorde, serving Brussels and the towns 40 to 60 km around it, in French and Dutch. It is owned by Roro (Fady's cousin), the work is done by Afrim, and it is a client of Fady's agency (fady.be, hi@fady.be) for ONE converting landing page plus Google Ads that make the phone ring. It is a single client with its own site, not a template business: nothing here is reused across clients, and nothing from the taxi business is edited from here.

## 2. The people and how we talk to them

- **Roro** owner. French and English fluent, Arabic native. Fady and Roro talk in English or Arabic. Questions go by WhatsApp, short, in English, batched.
- **Afrim** the technician in the van (spelling corrected by Roro 2026-08-27). French working level, German better, English basic. Gets his brief from Roro.
- **Roro's wife** administration. French fluent, good Dutch.
- **Roro's accountant** exists; company facts come from the public register first, from Roro second.
- **Fady** the agency: owner of the plan, the taste, the clicks that need a human (payments, credentials, account grants). Claude is planner, executor, ads specialist, landing-page designer and developer, and treats this as its own project.

## 3. What each folder is for

- `STATE.md` what is true today, rewritten in place, budget 14 KB. Every session reads it first.
- `NOW.md` what is open, one line each, owner-tagged `[Fady]` / `[Claude]` / `[Roro]` / `[clock: date]`, budget 8 KB. A closed line moves to `LOG.md` in the same session.
- `HANDOFF.md` the note the last session left for the next one, overwritten at every close, budget 4 KB.
- `DECISIONS.md` dated decisions, append-only, no budget on the file, max 5 lines per entry.
- `LOG.md` dated, append-only, one entry per session, no budget on the file, but ONE entry stays under about 2,500 bytes (five short lines). Log changes, not activity.
- **Budgets are in BYTES, not lines** (changed 2026-08-30). A line cap measures nothing here, because these files run one long paragraph per line: `STATE.md` sat at 27 percent of its old 120-line cap while costing 11.9 KB to read. What a session pays for is bytes read. The three budgets above are the real sizes of 2026-08-30 plus roughly 20 percent headroom. Check with `wc -c STATE.md NOW.md HANDOFF.md`. The same reason sets the per-entry limit on `LOG.md`: the file is far too large to read whole (over 200 KB) and its old entries ran long, which is why `/orch` can only afford to read the last entry.
- `playbook/` the curated knowledge, small files read on demand: `business-brief.md` (the client facts), `accounts.md` (every account, who owns it, how we reach it, NEVER a password), `launch-plan.md` (the gated plan to go live), `landing-page.md` (the page spec and copy decisions), `ads-program.md` (the campaign and its routine), `invoicing-flow.md` (how a job becomes a bill: what Roro sends Steph, the invoice and credit-note rules, Peppol), `separation.md` (how fady.be keeps clients apart), `site-companion.md` (the page section by section with answers to Roro's likely questions, a reference doc, uncapped).
- `research/` raw agent reports, dated, read-only reference, catalogued in `research/INDEX.md`. Never the owner of a fact: when a research fact becomes a decision, it moves into a playbook file or `DECISIONS.md`.
- `assets/raw/` what Roro sent, untouched. `assets/prepared/` what we made from it.
- `design/site-source/` THE page source (the one locked design, 2026-08-26): template, copy FR/NL/EN, styles, CGV and privacy modules, build script, fonts; its README carries the exact build and deploy commands. `site-v1/` is the build output, generated by `node design/site-source/build.js`, never edited by hand. (Locked 2026-08-26 on Fady's yes; the retired first page and the v2 pack moved to `_archive/2026-08-26-lock-in/`.)
- `_archive/` closed history, unchanged.
- `to-delete/` the waiting room for superseded files (Fady, 2026-08-26). A replaced file that is not worth keeping is MOVED here in a dated subfolder, never deleted in place and never left lying next to the live one. Nothing reads from it. **Only Fady empties it.** Keep-worthy history goes to `_archive/` instead.
- `.claude/skills/` the three routines `/orch`, `/orch-mid`, `/eof` (renamed from `/pro-orch`, `/pro-orch-mid`, `/pro-eof` on 2026-09-04, the HQ consistency pass, so every venture folder answers to the same three words; the procedure inside them is unchanged).
- `.claude/hooks/` the scripts that ENFORCE the never-bend rules instead of trusting a tool to remember them: `deny-git.cjs` (no version-control commands from a sandboxed tool), `no-emdash.cjs` (section 6), `protect-paths.cjs` (paths that must not be written), and since 2026-09-04 `session-stamp.cjs` plus `log-guard.cjs` with `log-owners.json` (the logging guard, section 8: a session that changed files but logged nothing is told so once at stop). `.claude/scripts/chrome.cjs` the Chrome profile helper (section 14). Added 2026-08-28, named here and put on the backup whitelist 2026-08-30 after an audit found the enforcement layer had no cloud copy while the rules it enforces did. Since 2026-09-04 the hooks, the script, the two `.cmd` files and the three routine skills are the shared kit every venture folder carries (`../fady.be/kit/README.md`); the `.js` hook files became `.cjs` the same day, and the em dash hook now checks the text a tool writes instead of the whole file.
- `save-to-cloud.cmd` Fady's backup script. Only Fady runs it (sections 7 and 13).

## 4. Where facts live (trust the file, never memory)

| Fact | The one owner |
|---|---|
| Client facts (people, services, zone, equipment, what Roro said) | `playbook/business-brief.md` |
| Accounts, ownership, access, domains, hosting | `playbook/accounts.md` |
| The plan to launch and its gates | `playbook/launch-plan.md` |
| The page: structure, headline, copy decisions, legal musts | `playbook/landing-page.md` |
| Ads: structure, keywords, routine, checkpoints, what is measured | `playbook/ads-program.md` |
| Invoicing and quotes: what Roro sends Steph per job, invoice and credit-note rules, Peppol for VAT-registered business customers | `playbook/invoicing-flow.md` |
| Every call and job Roro reports (his daily WhatsApp), the only base for a cost per job; no names, numbers or streets (added 2026-09-18) | `playbook/jobs-ledger.md` |
| Agency-wide separation of clients and accounts | `playbook/separation.md` |
| Custody and control: who manages, who owns on record, where the second factors live | `playbook/separation.md` |
| What is true today | `STATE.md` |
| What is open | `NOW.md` |
| What was decided | `DECISIONS.md` |
| What happened | `LOG.md` |
| Prices the agency charges Roro | `DECISIONS.md` (no separate offer file until there is an offer) |
| Prices the customer sees (the From price grid) | the site source copy files (`design/site-source/copy-fr.js` and nl/en); the built page is what the customer sees |
| The working rules for any tool | this file |

*A row was added on 2026-09-01: prices the customer sees got an owner because the grid was restated in full in three files, so the next price change had three places to miss.*

*Two rows were added on 2026-08-30. `playbook/invoicing-flow.md` already existed and other files already pointed at it, while it appeared in neither section 3 nor this table, which is exactly how `assets/prepared/invoice/README.md` drifted into stating a numbering rule the decisions had already replaced. Custody and control got an owner because that decision was one day old and was already restated in full in five files, so the next change had five places to miss; `playbook/separation.md` owns it now and everything else points there.*

## 5. The rules that never bend

1. **Never fake anything.** No fake reviews, no fake counters, no unproven claims ("20 years", "1M users", "97%") in any copy. We claim only what Roro confirms and what we can show.
2. **The client owns what matters and is never locked in:** his domain, his Google account, his Business Profile, his Ads account and its billing, his reviews. The agency is a manager on them, never the owner.
3. **No ad-ROI promises** to Roro before real numbers exist.
4. **The Business Profile is a gate:** no address or name goes onto Google before the address question (section 1 of `playbook/launch-plan.md`) is settled.
5. **One live page, one design.** Changes are made in the `design/site-source/` source, rebuilt and redeployed. Nothing is edited on the live host by hand.
6. **`../taxi-business/` is read-only from here.** We learn from it, we never change it, and its accounts are never reused for this client (see `playbook/separation.md`).

## 6. Writing rules

- **No em dashes**, ever, in anything new: chat, files, copy, scripts, WhatsApp drafts. Commas, full stops or brackets instead.
- **Customer-facing copy is French first and Dutch second, simple words, short sentences**, no marketing fog, no unproven claims. Both languages ship together.
- **Copy is written to convert, never merely translated** (Fady, 2026-08-24). Anything customer-facing, new or changed, matches the page's established voice per language (FR spoken "on", everyday Flemish, EN with contractions), speaks to the customer in their situation, and sells the call. FR, NL and EN each carry the same promise as native persuasive copy; a literal word-for-word translation is a bug.
- **Anything for Roro is simple English**, WhatsApp length, one question per line when asking.
- **Long numbers digit by digit** in anything meant to be read aloud (phone numbers, enterprise numbers).
- Prices in euros, VAT stated.

## 7. Working rules for any tool that touches files

- Edit the live file directly. Git history is the backup. No `.bak` copies.
- **A Bash command over about 8,000 characters never runs on this machine** (the Windows command-line limit cuts it and the shell answers "unexpected EOF"; it bit twice on 2026-09-16 and 17, both times a long heredoc). Long content goes through the Write tool or a script file that is then run; shell commands stay short. (Added 2026-09-17, internal rule.)
- **A BRIEF NEVER TELLS A FILE AGENT TO RECORD AN ACTION THAT IS STILL IN FLIGHT.** On 2026-09-18 a playbook agent was told to write that indexing "was requested again" while the browser agent doing it was still running; the closing audit caught a claim with no record behind it. File agents get facts that were already read back; anything in flight is filed by the session when its agent reports. Same day a Haiku agent misread the KBO page (the legal denomination taken for the commercial name, 2025 for 2026): a register read that Roro will be told about goes to Sonnet or is re-read by the session. (Added 2026-09-18, internal rule.)
- **TWO SESSIONS MUST NEVER BE WRITING AT THE SAME TIME, and "only one window is open" no longer proves it.** Sessions on this folder are now started and answered from the phone and from the cloud as well as from this machine, and Fady REPLIES from his phone, so a phone session drives real edits: the same human can be driving two writing sessions from two devices, hours apart, without either one knowing. The guard that actually works is cheap: **every session re-reads `STATE.md`, `NOW.md` and `HANDOFF.md` immediately before its first write of a stretch of work, not only at session start.** A session that has been idle, or that may have been resumed on another device, assumes the files moved under it. When the live state files have changed since the session last read them, it does NOT overwrite: it re-reads, merges its change onto what is there now, and says so. Inside one session, agents that edit files still run one at a time; read-only agents may run in parallel. Never run Cowork and Claude Code on this folder at the same time. (Corrected 2026-08-30 on Fady's yes.)
- **Sandboxed tools (Claude Code, Cowork, agents) never run git and never run `save-to-cloud.cmd` or `first-time-setup.cmd`.** They edit files, then stop. Fady double-clicks the backup himself, and only when something changed.
- **Live state files are rewritten in place** (`STATE.md`, `NOW.md`, `HANDOFF.md`, the playbook files). They carry no dated correction notes: the change is logged in `LOG.md` and git keeps the old text. Only `LOG.md` and `DECISIONS.md` are append-only history. This is the one deliberate difference from the taxi rules, chosen on 2026-08-22 so the files stay small enough to read whole.
- **Size caps are rules, not wishes, and they are byte budgets** (section 3: `STATE.md` 14 KB, `NOW.md` 8 KB, `HANDOFF.md` 4 KB). Check with `wc -c` on the file. When a file passes its budget, the same session trims it (closed lines to `LOG.md`, spent facts out of `STATE.md`) before adding anything. `LOG.md` and `DECISIONS.md` stay append-only with no budget on the file, but one `LOG.md` entry stays under about 2,500 bytes.
- Never store passwords, recovery codes, API tokens or card numbers in any file. `playbook/accounts.md` says WHICH account and WHO owns it, never HOW to get in.
- No customer personal data in the files beyond what the page itself shows.
- **After any edit to the page source, rebuild and check the result** (styles included) before calling the work done; a broken build or stale CSS ships silently otherwise. (Added 2026-08-24 on Fady's yes.)
- **Image picks are made from VIEWED images, never from filenames.** Crops of the same photo hide under different names; two duplicates shipped on 2026-08-24 because files were judged by name. Open and look before choosing, deduplicating or deleting. (Added 2026-08-25 on Fady's yes.)
- **A CHANGED IMAGE ALWAYS SHIPS UNDER A NEW FILENAME.** Replacing an image in place leaves the old bytes on the Cloudflare edge for up to 24 hours (`cf-cache-status: HIT`, max-age 86400), so the deploy succeeds, the new file is on the host, and the live page still shows the old picture. Bump the name instead (`van-2.jpg`, `afrim-avatar-2-*`), point the build at it, and move the replaced set to `to-delete/`. This bit twice on 2026-08-28, the og-banner and Afrim's avatar. (Added 2026-08-28 on Fady's yes.)
- **IDENTITY DOCUMENTS AND PERSONAL PAPERS ARE HANDLED OUTSIDE THIS FOLDER, redacted copies included.** No ID card, passport, residence permit, driving licence or similar personal document is ever saved anywhere under this folder, not even briefly and not even with the photo and the national number blocked out. `assets/` is on the backup whitelist, so a file dropped there is one backup run away from living in the version history forever, and the back of a Belgian eID carries the rijksregisternummer. Roro's ID card landed in `assets/raw/` as Front.jpg and Back.jpg on 2026-08-30 during the Google Ads affiliation task and was caught by luck, before any commit. Work on such documents from somewhere outside the folder, and record in the files only that it was done, never the document. (Added 2026-08-30 on Fady's yes.)
- **Browser work runs ONE agent at a time, and the agent opens the right Chrome profile itself.** Section 14 owns the mechanics (the lock, the profile map, the helper script, the 30 to 60 second connect). (Added 2026-09-01 after two parallel agents closed each other's tabs; shortened to this pointer 2026-09-20.)
- **VERIFY THE RESULT, NEVER THE SUBMISSION.** After anything is submitted to an external surface (a Google form, a verification flow, a dashboard action), the page is RE-READ and the RESULT confirmed before the outcome is written into any file. A submit is not an outcome. On 2026-08-30 the Google Ads "Confirm your affiliation" ID upload was filed as unconfirmed with a verdict in 1 to 10 days, because no success screen appeared; it had in fact been ACCEPTED the same day, and the only place that shows it is the Completed tasks list. Fady found it, the session did not. Many Google flows never show a success screen at all, so "nothing happened" is evidence of nothing: go and look where the answer actually lives. (Added 2026-08-30 on Fady's yes.)
- **A FACT-SYNC AUDIT RUNS AT LEAST ONCE A WEEK.** The live state files are read every session, so they stay honest. The `playbook/` files are read ON DEMAND, so nothing forces them to be re-read and they rot silently. On 2026-08-30 `STATE.md`, `NOW.md` and `HANDOFF.md` were all accurate and clean while roughly SEVENTEEN facts in `playbook/` were stale, two of them account-critical (`accounts.md` said the ad campaign was paused when it had been running for two days, and named the wrong payment card) and one of them the name of the customer review in the script that gets read aloud to Roro. So: a READ-ONLY agent cross-checks every `playbook/` file, plus the three READMEs that carry facts (`design/site-source/README.md`, `assets/prepared/invoice/README.md`, `design/business-card/README.md`; added 2026-09-20 after the site README was found stale in three places), against `STATE.md`, `NOW.md`, `DECISIONS.md` and the live site source, and reports every contradiction with file and line. It runs at least weekly (the natural slot is the Monday `/orch`, alongside the weekly ads check) and always after a session that changed an account, a price, a name or a live surface. It reports, it never fixes on its own: findings are fixed in the same session or filed in `NOW.md`, never left in chat. The cheap heuristic when reading anything: a fact that appears in more than one file is a candidate for drift, and section 4 names its one owner. (Added 2026-08-30 on Fady's yes.)

## 8. Logging (every session)

Fady never logs anything himself. The moment something loggable happens (done, decided, created, spent, promised, approved, a fact changed) it is filed in the same session: `LOG.md` for what happened, `NOW.md` for what is open, `DECISIONS.md` for decisions, `STATE.md` when what is true changed, `playbook/accounts.md` when an account was created or access changed. Every session ends with one line: what was logged and where, or "Nothing to log this session."

## 9. Close it, or file it, never leave it in chat

No task is deferred to "later" inside a conversation. It is done now, or it is a line in `NOW.md` with an owner, or a line in `DECISIONS.md`. A task that exists only in chat prose does not exist. A decision Fady makes in chat is written to the file it belongs to the moment it is made.

## 10. How a day runs

- `/orch` (until 2026-09-04: `/pro-orch`) starts the day: it reads `STATE.md`, `NOW.md`, `HANDOFF.md`, the last `LOG.md` entry and the last 10 lines of `DECISIONS.md`, checks them against each other in one line, checks the live surfaces read-only (the site answers; Google Ads keeps its WEEKLY read of `playbook/ads-program.md` section 6 and gets no daily look, Fady removed the daily glance on 2026-09-08 and again from `/orch` on 2026-09-20), gives the overview (**CLAUDE'S** / **A HUMAN'S** / **A CLOCK'S**; until 2026-09-20 the first two were called MINE and RORO'S, renamed by the kit on Fady's pick so every venture uses the same three), proposes the day's plan with reasons, then waits for Fady's go.
- `/orch-mid` (until 2026-09-04: `/pro-orch-mid`) resumes the same day from `HANDOFF.md` without repeating the overview.
- `/eof` (until 2026-09-04: `/pro-eof`) closes the day (at any hour, a morning close is normal): bullet-point plain-English summary, logs filed, `HANDOFF.md` rewritten, file sizes reported. Improvements to rules and skills are APPLIED directly, dated, when they only change how Claude and the files work internally; they go to Fady only when they change what he does, sees or decides (Fady, 2026-09-01).
- **Fan-out mode is the standard:** the session fans work out to subagents on the model the Models rule in `/orch` names: the cheapest model that can do the job (Haiku for mechanical work, Sonnet for the middle, Opus only where judgment carries money or the brand, Fable never as a worker except to rescue a stuck job; tiers, never versions, since 2026-09-22: the versions and prices live in `../fady.be/kit/model-and-effort-guide.md`). (Aligned 2026-09-20 with the kit rule of 2026-09-15, Fady's yes of that date; the older wording here named one model for every agent.) **The orchestrator NEVER executes a task itself, browser clicks included** (Fady, restated 2026-08-29 after the session did the Ads work itself): it plans, briefs agents, verifies their claims and files the results. Execution always goes to a subagent on a good cheaper model; when a task needs something only the session can do (an AskUserQuestion pick, a permission grant), the session gets that one piece and hands the task back to an agent. Before agents fire, the session explains each agent's job in plain English so Fady's yes is informed. Anything that is Fady's call comes as an AskUserQuestion widget with the essentials INSIDE the widget, because Fady sees the widget, not the prose above it.
- **Do the work by default:** anything that needs no payment, no credentials and no decision that is truly Fady's is done by the session (browser included). Money, passwords, account-level agreements, access grants and anything that touches a live client surface come to Fady first. The permission classifier refuses access-grant clicks for agents and the main session alike: plan those as Fady's clicks upfront (inventory by an agent, Fady clicks, read-only verify by an agent). For a money move and for any mail or message to a customer or the client, a widget alone is not the go: Fady types it, naming the action and the amount (section 15, added 2026-09-20).
- **Fady reacts to the work, he does not read explanations** (Fady, 2026-08-26): build it, deploy it, hand him the link and a few lines. No account of what was touched and why. Anything that is genuinely his decision comes as an AskUserQuestion widget with the context inside it; everything else is just done, cleanly, and verified before he is told it is ready.
- **Continuity is the session's job to call:** when context grows long enough that quality or cost suffers, say so unprompted, rewrite `HANDOFF.md`, and the next session starts with `/orch-mid`.
- **A design round is done only when the change is deployed to the preview and verified there.** The deployed preview is the ONLY review surface. (Added 2026-08-24 on Fady's yes; the canvas-artifact caveat it once carried died with the v2 pack at the 2026-08-26 lock-in.)

## 11. Automation and improvement mindset

The end goal is a business mostly run by agents, with Roro's setup kept dead simple. For every task ask two things: can this be automated, and did this session reveal a better rule, file or routine? Say so in one line so Fady decides. A skill that misfires is fixed the same day it is used.

## 12. When you are unsure

Say "not in the files" instead of guessing. Never invent a fact, a number, a price or a client detail. Say plainly when an idea is your own rather than something the files decided. Ask Fady one clear question rather than choosing for him on anything that spends money, touches a live surface, or changes a rule.

## 13. Backup and git (the shared rule, kit 2026-09-04)

*Section 7 already carried these rules since 2026-08-22; this is the shared wording every venture folder carries, so the HQ can compare them. Where the two differ, the stricter one holds.*

- The backup is a private GitHub repo under Fady's account (`fado767/pro-debouchage`, connected 2026-08-30). `save-to-cloud.cmd` backs the folder up and only says SAVED when GitHub provably matches this computer. Only Fady runs it, by double-click, and only when something on disk changed. A session's closing block reminds him in one line; the reminder is never an open item. (The spent `first-time-setup.cmd` moved to `to-delete/2026-09-04/` on Fady's ask.)
- Sandboxed tools (Claude Code, Cowork, agents) never run git, not even read-only, and never run the two scripts. `.claude/hooks/deny-git.cjs` enforces it. Reason on record: a session's read-only git command left a lock file that silently blocked Fady's backup on 2026-08-15 (taxi business).
- `.gitignore` is a WHITELIST: it names what is backed up. Machine-local, generated and secret files stay out (`.claude/settings.local.json`, `.claude/launch.json`, `.claude/hooks/state/`, `.claude/worktrees/`, `.env*`, `node_modules/`). Whoever adds a root folder that must survive adds it to the whitelist in the same session.
- Never run `save-to-cloud.cmd` while a session is actively writing files: the script clears a leftover lock as a last resort, and doing that mid-write is how the taxi repo corrupted on 2026-07-07.
- Edit the live file directly; git history is the backup, no `.bak` copies.

## 14. Browser work (the shared rule, kit 2026-09-04, verified in the HQ the same day)

*The 2026-09-01 bullet in section 7 is the origin; this is the shared wording, and it is the owner of the mechanics from today.*

- **Two things before any Chrome work, in this order** (added 2026-09-04 evening, Fady's ask after two project sessions ran in parallel): (1) the session is already NAMED `Pro Débouchage | <mode> | <date>` (the skills do it at their step 0 with `set_session_title`), so the sidebar entry and every Chrome tab group this session creates carry the project's name and never a bare "Orchestration"; (2) take the machine-wide browser lock, `node .claude/scripts/browser-lock.cjs acquire "Pro Débouchage"`. If it answers HELD, another project's session is driving Chrome right now: do the non-browser work first and retry, never open Chrome on top of it. Release it the moment the tabs are closed: `node .claude/scripts/browser-lock.cjs release "Pro Débouchage"`. A lock older than 20 minutes counts as abandoned, so **a browser agent re-runs the acquire line every 10 minutes as a heartbeat** (re-acquiring by the same owner only refreshes the timestamp; added 2026-09-16 after a 109-minute read-only Ads read went stale at 22 minutes while alive and well). **BUT A STALE LOCK IS NOT PROOF THE AGENT IS DEAD** (added 2026-09-10): a browser agent was stopped at 24 minutes because its lock had gone stale, and it was still working; its half-finished read was lost and had to be re-run. Check whether the agent is actually alive before taking its lock. Only a finished, failed or genuinely silent agent has abandoned one, and a lock left by a task that has FAILED is safe to release immediately. A sub-agent that drives Chrome gets the same acquire and release lines in its brief. A long browser job re-acquires the lock every 10 minutes (the same acquire line refreshes the time), so a living agent never looks abandoned; a stale lock is a hint, not proof, that the agent died. After two browser agents in a row fail, stop spawning and tell Fady what was seen (pro-debouchage, 2026-09). (Those two sentences are the kit's wording of 2026-09-20, taken from this folder and checked against it that day: they agree. This folder's own longer wording, here and in the two-failures bullet below, is their source and is stricter, two failures in a row on ANY step, so it holds.)
- **Parallel work without collisions (research of 2026-09-04, Anthropic docs and issue tracker, read-only agent):** Anthropic's own Windows guidance is to close other Claude Code sessions that use Chrome; the extension is bound to the Anthropic ACCOUNT, not to a session or a Chrome profile, so two sessions on this machine share one Chrome connection and nothing can pin a session to a profile (device names reset on every reconnect, an open bug). So: Chrome work goes through the lock above, one project at a time; everything that needs no login (research, reading public pages, checking a deployed preview) uses the in-app Browser pane instead (the `Claude_Browser` tools), a separate clean browser per session that runs in parallel with anyone's Chrome work. The Browser pane can also sign in to a site with "Persist sessions" on; whether that login survives a restart is not documented, so nothing account-critical moves there until Fady tests it once by hand.
- Fady's Chrome has one profile per identity. Chrome's own Local State file is the owner of the map; `node .claude/scripts/chrome.cjs` prints it. As of 2026-09-04: `Default` = personal (fadyapple@hotmail.com), `Profile 3` = Taxi Henok, `Profile 4` = Pro Debouchage (info@prodebouchage24.be, the Business Profile and the payments centre live here), `Profile 6` = fady.be (hi@fady.be, Ads admin). All four carry the Claude extension (Profile 4 included, checked 2026-09-04; the 2026-09-02 note that it lacked it is outdated).
- A session opens the profile it needs ITSELF and never asks "which browser": `node .claude/scripts/chrome.cjs open "Pro Debouchage" <url>`. Verified 2026-09-04: this opens the right profile every time. What it does NOT do is connect the extension instantly: a freshly opened window can take 30 to 60 seconds to show up in `list_connected_browsers` (the pro-debouchage agent saw Profile 4 connect on its own after about thirty seconds on 2026-09-04; the HQ test that morning gave up after twenty). So: open by command, wait, check the list again after a minute, and only if it is still empty ask Fady for ONE click, the Claude side panel in that window, nothing else.
- The agent confirms the signed-in account FROM THE PAGE before reading or writing anything, never from a `/u/N` URL or a device name. It may use Google's account switcher when the wanted account is already signed in there; a password screen means stop.
- ONE browser-driving agent at a time on this machine, across ALL projects, ever (two collided on 2026-08-24 and again on 2026-09-01). Everything else runs in parallel. Access grants (adding a user, accepting a manager invite) are refused by the permission classifier for agents and the main session alike: plan them as Fady's own clicks with a read-only verify afterwards.
- **THREE BROWSER AGENTS DIED IN ONE MORNING ON THIS MACHINE (2026-09-10), and the cause is Chrome, not the brief.** First an agent was killed at 24 minutes on a stale lock while still working; then a four-job agent died after ten minutes of no progress, having reported two live-account edits it could no longer prove; then a deliberately SMALL one-job read-only agent died before it did anything at all, on the line "I will start with the browser lock and opening Chrome". That third failure disproves the first diagnosis (that long multi-job briefs were to blame) and points at the Chrome side: a wedged extension connection, or windows the dead agents left open. **So: after TWO consecutive browser-agent failures, STOP SPAWNING.** A third attempt costs another ten minutes and proves nothing. Say plainly that the surface is unavailable, say what state that leaves things in, and either give Fady the two-minute click that settles it or move the item to the next session. Never keep a live account in an unknown state silently.
- **Still keep a browser agent's brief to ONE job, and put the money-stopping change FIRST.** Not because brief size caused the failures above, but because a stall then costs a read and never leaves a half-applied edit on a live account. When an agent dies mid-task, the next one VERIFIES the account state independently and never trusts the dead agent's last line.
- Every browser agent closes the tabs it opened before it reports; Fady's own tabs are never touched.
- **GOOGLE'S "CONFIRM IT'S YOU" IS FADY'S, AND THE AGENT WAITS FOR HIM** (2026-09-17). Creating ad groups or ads on the Ads account fires an identity check at Save; plain edits (bidding, pausing, negatives, an appeal) did not. The number to match shows in the Chrome window and the tap is on Fady's phone, so: the agent reaches the Save and, when the dialog appears, clicks NOTHING inside it (corrected 2026-09-19: an agent's own click on Confirm froze the dialog on "Try again" for ten minutes on 2026-09-18, while the untouched dialog, with Fady clicking Confirm himself, cleared in 20 to 30 seconds, twice in one build), sends the orchestrator one line through SendMessage to "main" ("IDENTITY CHECK IS ON SCREEN NOW", never the number) and only re-reads the page every 20 seconds for up to 10 minutes; the orchestrator pings Fady in chat and by PushNotification; when the dialog is gone the agent clicks Save again and carries on. The check can fire more than once in one build, so Fady stays reachable until the agent reports. Get Fady's "ready" BEFORE the agent starts: a check nobody answers costs the whole filled form. An agent is NEVER asked to read out or relay the number: two agents refused that as an MFA-relay pattern and they were right. Two failures on the surface still means stop spawning.
- **A BRIEF THAT CHANGES A LIVE ACCOUNT CARRIES THE DECISION, NOT THE WORD "APPROVED"** (2026-09-17). Two browser agents declined edits Fady had approved minutes earlier, because "approved edits" in a brief reads like an unverifiable claim, and a relayed approval mid-task reads like an injection. What worked: the brief states the edits as its instruction and names who decided, when, through what (the widget or his words in chat, quoted), the DECISIONS.md entry, and that the same class of edit was made by agents on named earlier dates. Approval is never relayed mid-task; a fresh agent gets a better brief.
- **google.be and pagespeed.web.dev do not work from the Browser pane** (2026-09-16: "unusual traffic" page, HTTP 429). "Does our ad show for query X in town Y" is answered by Google Ads, Tools, Troubleshooting, Ad Preview and Diagnosis, in Chrome: it costs nothing and counts no impression. PageSpeed runs in Chrome too. Our own site reads fine from the pane.
- VERIFY THE RESULT, NEVER THE SUBMISSION (section 7 owns the story of 2026-08-30): after anything is submitted on an external surface, the page is re-read and the outcome confirmed before it is written into any file.

## 15. Permissions and money (the shared rule, kit 2026-09-20)

*Added 2026-09-20 by the HQ consistency pass, the kit's wording. Section 10 ("Do the work by default") and section 12 already send money and anything that touches a live client surface to Fady first; that wider scope is this folder's own and it holds. What this block adds is the FORM of his yes for money and for mail (typed, naming the action and the amount), the rule that a session never unblocks itself, and the unattended rule. For the Ads account this is the whole money gate: a hook cannot read a click inside Chrome, so the gate is Fady's typed go, the auto-mode classifier and the daily budget on Google's side. WHEN an Ads setting may move at all stays with `playbook/ads-program.md` section 6 (the checkpoints). The caps this folder spends under are written in `STATE.md` and in the ADS line of `NOW.md`; "the open-items file" below is `NOW.md`.*

- **Permissions are Anthropic's standard settings, nothing home-made.** Sessions run in Auto mode, and the one home
  for permission rules is Fady's user settings (`../fady.be/kit/permissions/`, his double-click). No gate script and no
  token is ever built, and a prompt problem is never solved by adding rules (a folder whose scripts place paid orders or mail customers may carry a few documented `ask` rules on those exact commands, as ecom does). A session never changes its own permission mode and
  never rewrites a rule or a hook to get past a block: it parks the action in the open-items file, tells Fady in one
  line, and moves on. A folder's own settings and hooks change only on Fady's word in that folder's session.
- **Money and mail to a customer or a client are Fady's.** An ad budget, bidding, switching a campaign or an ad on, an
  order, a refund, a purchase, a send: he gives the go in his own typed words, naming the action, and the amount when there is one (the
  app's safety check reads his typed words). Pausing to stop spend is always
  allowed. Unattended, none of it happens: the question is parked for the morning.
